# platform-project

Project workspace and membership management. Owns `ProjectRecord`, `ProjectMember`, and
`MemberRole` in `platform.project.domain`, with `ProjectService` in
`platform.project.application` for project CRUD and editor/viewer access checks.
