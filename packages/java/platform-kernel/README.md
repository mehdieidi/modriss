# platform-kernel

Shared kernel types used across platform feature libraries and MDE runners. Public entry
points: `PlatformException` (HTTP-aware runtime failures) and `ModelLevel` (CIM/PIM/PSM level
enum with API naming helpers).
