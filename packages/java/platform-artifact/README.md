# platform-artifact

Generated MDE artifact storage and indexing. Domain types are `ArtifactRecord` and
`ArtifactIndexRecord`. `ArtifactService` in `platform.artifact.application` handles artifact
CRUD within a project after transformations or generation complete.
