# SQS and SNS resources

Messaging classes configure SQS queues, redrive behavior, queue policies, SNS topics, subscriptions, filters, and topic policies.

Source: `mde/metamodels/psm/awspsm-messaging.emf`.

## `SqsQueue`

An AWS SQS queue resource with delivery, retention, encryption, redrive, FIFO, and policy settings. It is the deployable realization of buffered asynchronous delivery.

Direct supertypes: `AwsResource`, `S3NotificationDestination`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                       | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted values and example                                                                                                         |
| ------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `queueName`                     | `String` [1]          | The queue name that identifies this sqs queue in the model and its generated AWS configuration. Semantic validation: `FifoQueueNameSuffix` (fifo queue name suffix) in `mde/validation/psm/rules/messaging.evl` the value must be present and non-blank. `StandardQueueShouldNotUseFifoSuffix` (standard queue should not use fifo suffix) in `mde/validation/psm/rules/messaging.evl` the value must be present and non-blank. Transformation role: ETL rule `Queue2SqsQueue` in `mde/transformations/pim-to-awspsm/data-messaging-events.etl` assigns or materializes this feature while refining `SqsQueue`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/naming.eol`. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `orders-events`.                   |
| `contentBasedDeduplication`     | `Boolean` [1]         | The boolean decision for content based deduplication on this sqs queue. It keeps an important design choice explicit for review and transformation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Either `true` or `false`. Example: `false`.                                                                                         |
| `deduplicationScope`            | `String` [1]          | The FIFO deduplication boundary, determining whether duplicate detection applies across the queue or within each message group.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `orders`.                          |
| `fifoThroughputLimit`           | `String` [1]          | The FIFO throughput scope, choosing whether throughput is limited for the whole queue or per message group. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Sqs Queue Fifo Throughput Limit`. |
| `delaySeconds`                  | `Integer` [1]         | Stores the delay seconds duration or limit, expressed in seconds on the sqs queue. The field records an operational boundary explicitly instead of leaving it to provider defaults. Semantic validation: `QueueTimingRangesValid` (queue timing ranges valid) in `mde/validation/psm/rules/messaging.evl` the value must remain absent in this modeling situation. Transformation role: ETL rule `Queue2SqsQueue` in `mde/transformations/pim-to-awspsm/data-messaging-events.etl` assigns or materializes this feature while refining `SqsQueue`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                     | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                    |
| `maximumMessageSize`            | `Integer` [1]         | The numeric value used for maximum message size on this sqs queue. Keeping the quantity in the model lets validation and generation apply the same boundary consistently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                    |
| `messageRetentionPeriodSeconds` | `Integer` [1]         | The numeric value used for message retention period seconds on this sqs queue. Keeping the quantity in the model lets validation and generation apply the same boundary consistently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                    |
| `receiveMessageWaitTimeSeconds` | `Integer` [1]         | The numeric value used for receive message wait time seconds on this sqs queue. Keeping the quantity in the model lets validation and generation apply the same boundary consistently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                    |
| `visibilityTimeoutSeconds`      | `Integer` [1]         | Stores the visibility timeout seconds duration or limit, expressed in seconds on the sqs queue. The field records an operational boundary explicitly instead of leaving it to provider defaults. Semantic validation: `QueueTimingRangesValid` (queue timing ranges valid) in `mde/validation/psm/rules/messaging.evl` the value must remain absent in this modeling situation. Transformation role: ETL rule `Queue2SqsQueue` in `mde/transformations/pim-to-awspsm/data-messaging-events.etl` assigns or materializes this feature while refining `SqsQueue`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                        | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `30`.                                   |
| `kmsDataKeyReusePeriodSeconds`  | `Integer` [1]         | The numeric value used for kms data key reuse period seconds on this sqs queue. Keeping the quantity in the model lets validation and generation apply the same boundary consistently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                    |
| `sqsManagedSseEnabled`          | `Boolean` [1]         | Whether sqs managed sse is enabled for this sqs queue. The flag turns an operational choice into explicit model data. Semantic validation: `ProductionQueueShouldBeEncrypted` (production queue should be encrypted) in `mde/validation/psm/rules/messaging.evl` the related value or object must be explicitly provided. Transformation role: ETL rule `Queue2SqsQueue` in `mde/transformations/pim-to-awspsm/data-messaging-events.etl` assigns or materializes this feature while refining `SqsQueue`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                                                              | Either `true` or `false`. Example: `true`.                                                                                          |
| `queueType`                     | `SqsQueueType` [1]    | The controlled value used for queue type on this sqs queue. It keeps the distinction explicit in the abstract syntax so later rules can interpret it consistently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Exactly one of: `STANDARD`, `FIFO`. Example: `STANDARD`.                                                                            |

### Relationships

| Relationship                                   | Kind and multiplicity             | Meaning in the model                                                                                                                                                                                                                 |
| ---------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `redrivePolicy` → `SqsRedrivePolicy`           | containment, [?]                  | The `redrivePolicy` containment on `SqsQueue` attaches the access or governance decision represented by redrive policy. The `SqsRedrivePolicy` objects are owned by `SqsQueue` and remain part of its model subtree.                 |
| `redriveAllowPolicy` → `SqsRedriveAllowPolicy` | containment, [?]                  | The `redriveAllowPolicy` containment on `SqsQueue` attaches the access or governance decision represented by redrive allow policy. The `SqsRedriveAllowPolicy` objects are owned by `SqsQueue` and remain part of its model subtree. |
| `kmsKey` → `KmsKey`                            | reference, [?]                    | The `kmsKey` reference on `SqsQueue` selects the KMS key used for provider-side encryption. A `KmsKey` can remain independently owned and can participate in other parts of the model.                                               |
| `queuePolicies` → `SqsQueuePolicy`             | reference, [*]; opposite `queues` | The policies attached to this queue from the reverse side of the association.                                                                                                                                                        |

## `SqsRedrivePolicy`

`SqsRedrivePolicy` is an AWS messaging record in the AWS platform-specific model. It carries the delivery setting for sqs redrive policy. Its declaration gives the concept a precise home through dead letter queue. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute         | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                                  | Accepted values and example                                                                      |
| ----------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `maxReceiveCount` | `Integer` [1]         | Stores the max receive count value on the sqs redrive policy. The field records an operational boundary explicitly instead of leaving it to provider defaults. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`. |

### Relationships

| Relationship                   | Kind and multiplicity | Meaning in the model                                                                                       |
| ------------------------------ | --------------------- | ---------------------------------------------------------------------------------------------------------- |
| `deadLetterQueue` → `SqsQueue` | reference, [1]        | The SQS queue that receives messages or events after normal delivery or processing can no longer continue. |

## `SqsRedriveAllowPolicy`

`SqsRedriveAllowPolicy` is an AWS messaging record in the AWS platform-specific model. It carries the delivery setting for sqs redrive allow policy. Its declaration gives the concept a precise home through source queues. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute           | Type and multiplicity | What it captures and why it exists                                                                                                                                                                   | Accepted values and example                                                                                                                     |
| ------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `redrivePermission` | `String` [1]          | The scope of queues allowed to use this queue as their dead-letter destination. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Sqs Redrive Allow Policy Redrive Permission`. |

### Relationships

| Relationship                | Kind and multiplicity | Meaning in the model                                                                                                                                                                                     |
| --------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sourceQueues` → `SqsQueue` | reference, [*]        | The `sourceQueues` reference on `SqsRedriveAllowPolicy` identifies the source represented by source queues. A `SqsQueue` can remain independently owned and can participate in other parts of the model. |

## `SqsQueuePolicy`

`SqsQueuePolicy` is an AWS messaging record in the AWS platform-specific model. It carries the delivery setting for sqs queue policy. Its declaration gives the concept a precise home through policy document, queues. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute            | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                            | Accepted values and example                                                                                          |
| -------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `policyDocumentJson` | `String` [1]          | A serialized policy document retained for provider features that are still represented as JSON. It preserves the exact policy payload used by generation. Within `SqsQueuePolicy`, it applies to this specific element and its role in the surrounding model. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`. |

### Relationships

| Relationship                           | Kind and multiplicity                    | Meaning in the model                                                                                                                                                                                                   |
| -------------------------------------- | ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `policyDocument` → `IamPolicyDocument` | containment, [?]                         | The `policyDocument` containment on `SqsQueuePolicy` owns the structured IAM policy document used by the resource. The `IamPolicyDocument` objects are owned by `SqsQueuePolicy` and remain part of its model subtree. |
| `queues` → `SqsQueue`                  | reference, [+]; opposite `queuePolicies` | The SQS queues governed by this queue policy.                                                                                                                                                                          |

## `SnsTopic`

An AWS SNS topic resource that publishes messages to subscriptions. Its policy, encryption, ordering, filtering, and delivery settings define the fan-out boundary.

Direct supertypes: `AwsResource`, `S3NotificationDestination`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                   | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted values and example                                                                                          |
| --------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| `topicName`                 | `String` [1]          | The topic name that identifies this sns topic in the model and its generated AWS configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `orders-events`.    |
| `fifoTopic`                 | `Boolean` [1]         | The boolean decision for fifo topic on this sns topic. It keeps an important design choice explicit for review and transformation. Semantic validation: `FifoTopicNameSuffix` (fifo topic name suffix) in `mde/validation/psm/rules/messaging.evl` the flag must be enabled for this rule to pass. Transformation role: ETL rule `Topic2SnsTopic` in `mde/transformations/pim-to-awspsm/data-messaging-events.etl` assigns or materializes this feature while refining `SnsTopic`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                  | Either `true` or `false`. Example: `false`.                                                                          |
| `contentBasedDeduplication` | `Boolean` [1]         | The boolean decision for content based deduplication on this sns topic. It keeps an important design choice explicit for review and transformation. Transformation role: ETL rule `Topic2SnsTopic` in `mde/transformations/pim-to-awspsm/data-messaging-events.etl` assigns or materializes this feature while refining `SnsTopic`.                                                                                                                                                                                                                                                                                                                                      | Either `true` or `false`. Example: `false`.                                                                          |
| `displayName`               | `String` [1]          | The human-facing label for this element. It allows the editor and documentation to present a useful name while the model keeps its stable identity separately. Within `SnsTopic`, it applies to this specific element and its role in the surrounding model. Transformation role: ETL rule `Topic2SnsTopic` in `mde/transformations/pim-to-awspsm/data-messaging-events.etl` assigns or materializes this feature while refining `SnsTopic`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/naming.eol`. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `SnsTopicExample`.  |
| `dataProtectionPolicyJson`  | `String` [1]          | The serialized representation of data protection policy on this sns topic. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`. |
| `deliveryStatusLoggingJson` | `String` [1]          | The serialized representation of delivery status logging on this sns topic. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`. |

### Relationships

| Relationship                        | Kind and multiplicity             | Meaning in the model                                                                                                                                                                                                                                                         |
| ----------------------------------- | --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `kmsKey` → `KmsKey`                 | reference, [?]                    | The `kmsKey` reference on `SnsTopic` selects the KMS key used for provider-side encryption. A `KmsKey` can remain independently owned and can participate in other parts of the model.                                                                                       |
| `subscriptions` → `SnsSubscription` | reference, [*]; opposite `topic`  | The `subscriptions` reference on `SnsTopic` keeps the consumers enrolled in the topic or channel. A `SnsSubscription` can remain independently owned and can participate in other parts of the model. Its opposite `topic` exposes the same connection from the target side. |
| `topicPolicies` → `SnsTopicPolicy`  | reference, [*]; opposite `topics` | The policies attached to this topic from the reverse side of the association.                                                                                                                                                                                                |

## `SnsSubscription`

`SnsSubscription` is an AWS messaging record in the AWS platform-specific model. It carries the delivery setting for sns subscription. Its declaration gives the concept a precise home through filter rules, topic, endpoint resource. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute            | Type and multiplicity      | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted values and example                                                                                                   |
| -------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- |
| `endpoint`           | `String` [1]               | The destination address interpreted according to `protocol`, such as a queue ARN, Lambda ARN, email address, or HTTPS URL. Semantic validation: `SubscriptionHasEndpointOrResource` (subscription has endpoint or resource) in `mde/validation/psm/rules/messaging.evl` the value must be present and non-blank. Transformation role: ETL rule `Subscription2SnsSubscription` in `mde/transformations/pim-to-awspsm/data-messaging-events.etl` assigns or materializes this feature while refining `SnsSubscription`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Sns Subscription Endpoint`. |
| `rawMessageDelivery` | `Boolean` [1]              | The boolean decision for raw message delivery on this sns subscription. It keeps an important design choice explicit for review and transformation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Either `true` or `false`. Example: `false`.                                                                                   |
| `deliveryPolicyJson` | `String` [1]               | The serialized representation of delivery policy on this sns subscription. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.          |
| `replayPolicyJson`   | `String` [1]               | The serialized representation of replay policy on this sns subscription. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.          |
| `filterPolicyScope`  | `SnsFilterPolicyScope` [1] | Records boundary within which filter policy scope is interpreted for sns subscription. It keeps isolation and ownership decisions explicit during review and transformation, so later steps do not have to infer it. Transformation role: ETL rule `Subscription2SnsSubscription` in `mde/transformations/pim-to-awspsm/data-messaging-events.etl` assigns or materializes this feature while refining `SnsSubscription`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                             | Exactly one of: `MESSAGE_ATTRIBUTES`, `MESSAGE_BODY`. Example: `MESSAGE_ATTRIBUTES`.                                          |
| `protocol`           | `SnsProtocol` [1]          | The controlled value used for protocol on this sns subscription. It keeps the distinction explicit in the abstract syntax so later rules can interpret it consistently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Exactly one of: `LAMBDA`, `SQS`, `HTTP`, `HTTPS`, `EMAIL`, `SMS`, `FIREHOSE`, `APPLICATION`. Example: `LAMBDA`.               |

### Relationships

| Relationship                       | Kind and multiplicity                    | Meaning in the model                                                                                                                                                                                                     |
| ---------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `filterRules` → `SnsFilterRule`    | containment, [*]                         | The structured rules used to derive the SNS subscription filter policy.                                                                                                                                                  |
| `topic` → `SnsTopic`               | reference, [1]; opposite `subscriptions` | The SNS topic used as the publisher, target, or protected resource in this relationship.                                                                                                                                 |
| `endpointResource` → `AwsResource` | reference, [?]                           | The `endpointResource` reference on `SnsSubscription` identifies the provider resource at the far end of this endpoint. An `AwsResource` can remain independently owned and can participate in other parts of the model. |
| `deadLetterQueue` → `SqsQueue`     | reference, [?]                           | The SQS queue that receives messages or events after normal delivery or processing can no longer continue.                                                                                                               |

## `SnsFilterRule`

`SnsFilterRule` is an AWS messaging record in the AWS platform-specific model. It carries the delivery setting for sns filter rule. Its declaration gives the concept a precise home through its declared properties. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute   | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                           | Accepted values and example                                                                                           |
| ----------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `fieldPath` | `String` [1]          | The message attribute or payload path whose value is tested by this filter rule.                                                                                                                                                             | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `/orders/{orderId}`. |
| `operator`  | `String` [1]          | The comparison operation applied between the selected field and this rule's values. Semantic validation: `FilterRuleHasValues` (filter rule has values) in `mde/validation/psm/rules/messaging.evl` the value must be present and non-blank. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `>=`.                |
| `values`    | `String` [*]          | The operands accepted or compared by this subscription filter rule. Semantic validation: `FilterRuleHasValues` (filter rule has values) in `mde/validation/psm/rules/messaging.evl` the value must be present and non-blank.                 | A collection of values. Example: [`READY`, `READY-2`].                                                                |

### Relationships

This class declares no direct relationships.

## `SnsTopicPolicy`

`SnsTopicPolicy` is an AWS messaging record in the AWS platform-specific model. It carries the delivery setting for sns topic policy. Its declaration gives the concept a precise home through policy document, topics. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute            | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                            | Accepted values and example                                                                                          |
| -------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `policyDocumentJson` | `String` [1]          | A serialized policy document retained for provider features that are still represented as JSON. It preserves the exact policy payload used by generation. Within `SnsTopicPolicy`, it applies to this specific element and its role in the surrounding model. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`. |

### Relationships

| Relationship                           | Kind and multiplicity                    | Meaning in the model                                                                                                                                                                                                   |
| -------------------------------------- | ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `policyDocument` → `IamPolicyDocument` | containment, [?]                         | The `policyDocument` containment on `SnsTopicPolicy` owns the structured IAM policy document used by the resource. The `IamPolicyDocument` objects are owned by `SnsTopicPolicy` and remain part of its model subtree. |
| `topics` → `SnsTopic`                  | reference, [+]; opposite `topicPolicies` | The SNS topics governed by this topic policy.                                                                                                                                                                          |
