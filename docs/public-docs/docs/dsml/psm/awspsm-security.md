# IAM, KMS, Secrets Manager, and SSM

These classes map security intent into IAM documents, roles, KMS keys, Secrets Manager secrets, and Systems Manager parameters.

Source: `mde/metamodels/psm/awspsm-security.emf`.

## `IamRole`

An AWS IAM role that a service or trusted principal can assume. Its trust policy, inline policies, managed policies, and naming explain the permissions boundary used by generated resources.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepted values and example                                                                                                                               |
| ------------------------ | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `roleName`               | `String` [1]          | The role name that identifies this iam role in the model and its generated AWS configuration.                                                                                                                                                                                                                                                                                                                                                                                                                | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `OrdersRuntimeRole`.                                     |
| `path`                   | `String` [1]          | The IAM hierarchy path under which the role is created. Transformation role: ETL rule `Principal2IamRole` in `mde/transformations/pim-to-awspsm/workflow-security-config.etl` assigns or materializes this feature while refining `IamRole`.                                                                                                                                                                                                                                                                 | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `/application/`.                                         |
| `permissionsBoundaryArn` | `String` [1]          | Stores the boundary within which permissions boundary arn is interpreted on the iam role. The field records isolation and ownership decisions as an explicit, reviewable input. Semantic validation: `ProductionRoleShouldUsePermissionsBoundary` (production role should use permissions boundary) in `mde/validation/psm/rules/security.evl` the value must be present and non-blank. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `arn:aws:lambda:us-east-1:123456789012:function:orders`. |
| `maxSessionDuration`     | `Integer` [1]         | The maximum lifetime, in seconds, of a session assumed through this IAM role. Transformation role: ETL rule `Principal2IamRole` in `mde/transformations/pim-to-awspsm/workflow-security-config.etl` assigns or materializes this feature while refining `IamRole`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                      | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `30`.                                                         |
| `descriptionText`        | `String` [1]          | The provider-facing descriptive text emitted for this resource. It gives an operator context in the generated template and console without carrying runtime behavior. Within `IamRole`, it applies to this specific element and its role in the surrounding model.                                                                                                                                                                                                                                           | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Orders processing`.                                     |
| `managedPolicyArns`      | `String` [*]          | The ARNs of externally defined managed policies attached directly to the role. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                                                                                                                                                          | A collection of values. Example: [`arn:aws:lambda:us-east-1:123456789012:function:orders`, `arn:aws:lambda:us-east-1:123456789012:function:orders-2`].    |

### Relationships

| Relationship                             | Kind and multiplicity | Meaning in the model                                                                                                                                                                                                       |
| ---------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `assumeRolePolicy` → `IamPolicyDocument` | containment, [1]      | The `assumeRolePolicy` containment on `IamRole` attaches the access or governance decision represented by assume role policy. The `IamPolicyDocument` objects are owned by `IamRole` and remain part of its model subtree. |
| `inlinePolicies` → `IamInlinePolicy`     | containment, [*]      | Policies embedded in and lifecycle-bound to the IAM role.                                                                                                                                                                  |
| `managedPolicies` → `IamManagedPolicy`   | reference, [*]        | Modelled managed policies attached to the IAM role.                                                                                                                                                                        |

## `IamInlinePolicy`

`IamInlinePolicy` is an AWS security or configuration record in the AWS platform-specific model. It carries the provider setting for iam inline policy. Its declaration gives the concept a precise home through document. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute    | Type and multiplicity | What it captures and why it exists                                                                       | Accepted values and example                                                                                  |
| ------------ | --------------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `policyName` | `String` [1]          | The policy name that identifies this iam inline policy in the model and its generated AWS configuration. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `STANDARD`. |

### Relationships

| Relationship                     | Kind and multiplicity | Meaning in the model                                                           |
| -------------------------------- | --------------------- | ------------------------------------------------------------------------------ |
| `document` → `IamPolicyDocument` | containment, [1]      | The structured IAM policy document represented by this policy-bearing element. |

## `IamPolicy`

`IamPolicy` is an AWS security or configuration record in the AWS platform-specific model. It carries the provider setting for iam policy. Its declaration gives the concept a precise home through document, roles. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute       | Type and multiplicity | What it captures and why it exists                                                                                                                         | Accepted values and example                                                                                          |
| --------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `policyName`    | `String` [1]          | The policy name that identifies this iam policy in the model and its generated AWS configuration.                                                          | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `STANDARD`.         |
| `rawPolicyJson` | `String` [1]          | The serialized representation of raw policy on this iam policy. It carries provider-specific structure that is intentionally retained as document content. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`. |

### Relationships

| Relationship                     | Kind and multiplicity | Meaning in the model                                                           |
| -------------------------------- | --------------------- | ------------------------------------------------------------------------------ |
| `document` → `IamPolicyDocument` | containment, [1]      | The structured IAM policy document represented by this policy-bearing element. |
| `roles` → `IamRole`              | reference, [*]        | The IAM roles to which this standalone policy is attached.                     |

## `IamManagedPolicy`

`IamManagedPolicy` is an AWS security or configuration record in the AWS platform-specific model. It carries the provider setting for iam managed policy. Its declaration gives the concept a precise home through document. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute           | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                     | Accepted values and example                                                                                           |
| ------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `managedPolicyName` | `String` [1]          | The managed policy name that identifies this iam managed policy in the model and its generated AWS configuration. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `STANDARD`.          |
| `path`              | `String` [1]          | The IAM hierarchy path used for the managed policy.                                                                                                                                                                                    | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `/orders/{orderId}`. |
| `rawPolicyJson`     | `String` [1]          | The serialized representation of raw policy on this iam managed policy. It carries provider-specific structure that is intentionally retained as document content.                                                                     | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.  |

### Relationships

| Relationship                     | Kind and multiplicity | Meaning in the model                                                           |
| -------------------------------- | --------------------- | ------------------------------------------------------------------------------ |
| `document` → `IamPolicyDocument` | containment, [1]      | The structured IAM policy document represented by this policy-bearing element. |

## `IamPolicyDocument`

The structured IAM policy document carried by one or more AWS resources. It contains statements, principals, conditions, actions, resources, and effects before JSON generation.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute | Type and multiplicity | What it captures and why it exists                              | Accepted values and example                                                                             |
| --------- | --------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `version` | `String` [1]          | The IAM policy-language version used to interpret the document. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `1.0`. |

### Relationships

| Relationship                  | Kind and multiplicity | Meaning in the model                                                                                                                                                                                            |
| ----------------------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `statements` → `IamStatement` | containment, [+]      | The `statements` containment on `IamPolicyDocument` owns the IAM statements that make up the policy document. The `IamStatement` objects are owned by `IamPolicyDocument` and remain part of its model subtree. |

## `IamStatement`

`IamStatement` is an AWS security or configuration record in the AWS platform-specific model. It carries the provider setting for iam statement. Its declaration gives the concept a precise home through principals, conditions. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute               | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted values and example                                                                                                                          |
| ----------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sid`                   | `String` [1]          | An optional statement identifier used to distinguish and discuss one rule inside a policy document.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `ReadOrdersTable`.                                  |
| `actions`               | `String` [*]          | The allowed or denied IAM API operations. Keeping actions explicit is the main least-privilege surface of the statement. Semantic validation: `NoAllowWildcardInProductionWithoutJustification` (no allow wildcard in production without justification) in `mde/validation/psm/rules/security.evl` the rule's diagnostic or remediation guidance refers to this feature. `AvoidNotActionInAllowStatements` (avoid not action in allow statements) in `mde/validation/psm/rules/security.evl` the rule's diagnostic or remediation guidance refers to this feature. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/awspsm2artifacts.egx`. The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. | A collection of values. Example: [`dynamodb:GetItem`, `dynamodb:GetItem-2`].                                                                         |
| `notActions`            | `String` [*]          | The IAM operations excluded from the statement's effect. It is a powerful negative permission and therefore receives stricter review than an ordinary action list. Semantic validation: `AvoidNotActionInAllowStatements` (avoid not action in allow statements) in `mde/validation/psm/rules/security.evl` the feature participates in a semantic validation condition. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/iam.eol`.                                                                                                                                                                                                                                                                                                        | A collection of values. Example: [`Iam Statement Not Actions`, `Iam Statement Not Actions-2`].                                                       |
| `resources`             | `String` [*]          | The ARNs to which the actions apply. Resource scope is what turns an otherwise broad action into a least-privilege permission. Semantic validation: `NoAllowWildcardInProductionWithoutJustification` (no allow wildcard in production without justification) in `mde/validation/psm/rules/security.evl` the rule's diagnostic or remediation guidance refers to this feature. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/awspsm2artifacts.egx`. The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                                     | A collection of values. Example: [`arn:aws:dynamodb:us-east-1:123456789012:table/orders`, `arn:aws:dynamodb:us-east-1:123456789012:table/orders-2`]. |
| `notResources`          | `String` [*]          | The resources excluded from the statement. Negative resource scope must be justified because it is easy to misunderstand during audit. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/iam.eol`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A collection of values. Example: [`Iam Statement Not Resources`, `Iam Statement Not Resources-2`].                                                   |
| `wildcardAction`        | `Boolean` [1]         | The boolean decision for wildcard action on this iam statement. It keeps an important design choice explicit for review and transformation. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/validation.eol`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Either `true` or `false`. Example: `false`.                                                                                                          |
| `wildcardResource`      | `Boolean` [1]         | The boolean decision for wildcard resource on this iam statement. It keeps an important design choice explicit for review and transformation. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/validation.eol`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Either `true` or `false`. Example: `false`.                                                                                                          |
| `wildcardJustification` | `String` [1]          | The written reason a wildcard permission is safe and necessary, providing the evidence required for least-privilege review. Semantic validation: `NoAllowWildcardInProductionWithoutJustification` (no allow wildcard in production without justification) in `mde/validation/psm/rules/security.evl` the rule's diagnostic or remediation guidance refers to this feature. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/validation.eol`. The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/templates/infrastructure/iam-policy-rationale.egl`.                                                                                                                                                    | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Iam Statement Wildcard Justification`.             |
| `effect`                | `IamEffect` [1]       | Whether the IAM statement grants the listed capabilities or explicitly denies them. Semantic validation: `NoAllowWildcardInProductionWithoutJustification` (no allow wildcard in production without justification) in `mde/validation/psm/rules/security.evl` the feature participates in a semantic validation condition. `AvoidNotActionInAllowStatements` (avoid not action in allow statements) in `mde/validation/psm/rules/security.evl` the feature participates in a semantic validation condition. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/iam.eol`. The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/values.eol`.                                                              | Exactly one of: `ALLOW`, `DENY`. Example: `ALLOW`.                                                                                                   |

### Relationships

| Relationship                  | Kind and multiplicity | Meaning in the model                                                                                                                                                                                         |
| ----------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `principals` → `IamPrincipal` | containment, [*]      | The `principals` containment on `IamStatement` identifies the identities to which the policy statement applies. The `IamPrincipal` objects are owned by `IamStatement` and remain part of its model subtree. |
| `conditions` → `IamCondition` | containment, [*]      | The `conditions` containment on `IamStatement` keeps the IAM conditions that narrow when a statement matches. The `IamCondition` objects are owned by `IamStatement` and remain part of its model subtree.   |

## `IamPrincipal`

`IamPrincipal` is an AWS security or configuration record in the AWS platform-specific model. It carries the provider setting for iam principal. Its declaration gives the concept a precise home through its declared properties. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute       | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted values and example                                                                                  |
| --------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `principalType` | `String` [1]          | Records accountable person, team, or identity for principal type for iam principal. It keeps accountability and access review explicit during review and transformation, so later steps do not have to infer it. Semantic validation: `PrincipalHasTypeAndIdentifiers` (principal has type and identifiers) in `mde/validation/psm/rules/security.evl` the value must be present and non-blank. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/iam.eol`. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `BUSINESS`. |
| `identifiers`   | `String` [*]          | The accounts, users, roles, services, or federated identities included under this principal type.                                                                                                                                                                                                                                                                                                                                                                                                                    | A collection of values. Example: [`order-123`, `order-123-2`].                                               |

### Relationships

This class declares no direct relationships.

## `IamCondition`

`IamCondition` is an AWS security or configuration record in the AWS platform-specific model. It carries the provider setting for iam condition. Its declaration gives the concept a precise home through its declared properties. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute  | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                           | Accepted values and example                                                                                 |
| ---------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `operator` | `String` [1]          | The IAM condition operator used to compare the request context with the configured values. Semantic validation: `ConditionComplete` (condition complete) in `mde/validation/psm/rules/security.evl` the value must be present and non-blank. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `>=`.      |
| `key`      | `String` [1]          | The IAM request-context key examined by this condition.                                                                                                                                                                                      | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `orderId`. |
| `values`   | `String` [*]          | The expected values supplied to the condition operator. Semantic validation: `ConditionComplete` (condition complete) in `mde/validation/psm/rules/security.evl` the value must be present and non-blank.                                    | A collection of values. Example: [`READY`, `READY-2`].                                                      |

### Relationships

This class declares no direct relationships.

## `KmsKey`

An AWS KMS key used to protect data or configuration. Its rotation, deletion, multi-region, key policy, and alias settings make encryption a concrete deployment decision.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute             | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted values and example                                                                                           |
| --------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `descriptionText`     | `String` [1]          | The provider-facing descriptive text emitted for this resource. It gives an operator context in the generated template and console without carrying runtime behavior. Within `KmsKey`, it applies to this specific element and its role in the surrounding model.                                                                                                                                                                                                                                                                           | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Orders processing`. |
| `keyUsage`            | `String` [1]          | The cryptographic purpose allowed for the key, which separates encryption keys from signing or verification keys. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                                                                                                                                                      | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `orderId`.           |
| `keySpec`             | `String` [1]          | The key-material specification that fixes the cryptographic family and key size.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `orderId`.           |
| `enabled`             | `Boolean` [1]         | Whether this capability or connection is active in the modeled scenario. Keeping the switch explicit distinguishes an intentional omission from a temporarily disabled design. Within `KmsKey`, it applies to this specific element and its role in the surrounding model.                                                                                                                                                                                                                                                                  | Either `true` or `false`. Example: `true`.                                                                            |
| `enableKeyRotation`   | `Boolean` [1]         | The boolean decision for enable key rotation on this kms key. It keeps an important design choice explicit for review and transformation.                                                                                                                                                                                                                                                                                                                                                                                                   | Either `true` or `false`. Example: `false`.                                                                           |
| `multiRegion`         | `Boolean` [1]         | The boolean decision for multi region on this kms key. It keeps an important design choice explicit for review and transformation.                                                                                                                                                                                                                                                                                                                                                                                                          | Either `true` or `false`. Example: `false`.                                                                           |
| `pendingWindowInDays` | `Integer` [1]         | Records pending window in days duration or limit, expressed in days for kms key. It keeps an operational boundary that should not be left to provider defaults explicit during review and transformation, so later steps do not have to infer it. Semantic validation: `PendingWindowRange` (pending window range) in `mde/validation/psm/rules/security.evl` the value must remain absent in this modeling situation. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                      |
| `keyPolicyJson`       | `String` [1]          | The serialized representation of key policy on this kms key. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                                                                                                                                                     | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.  |

### Relationships

| Relationship                      | Kind and multiplicity                | Meaning in the model                                                                                                                                                                                                                                                   |
| --------------------------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `keyPolicy` → `IamPolicyDocument` | containment, [?]                     | The `keyPolicy` containment on `KmsKey` attaches the access or governance decision represented by key policy. The `IamPolicyDocument` objects are owned by `KmsKey` and remain part of its model subtree.                                                              |
| `aliases` → `KmsAlias`            | reference, [*]; opposite `targetKey` | The `aliases` reference on `KmsKey` keeps the named aliases that make the KMS key addressable. A `KmsAlias` can remain independently owned and can participate in other parts of the model. Its opposite `targetKey` exposes the same connection from the target side. |

## `KmsAlias`

`KmsAlias` is an AWS security or configuration record in the AWS platform-specific model. It carries the provider setting for kms alias. Its declaration gives the concept a precise home through target key. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute   | Type and multiplicity | What it captures and why it exists                                                              | Accepted values and example                                                                                          |
| ----------- | --------------------- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `aliasName` | `String` [1]          | The alias name that identifies this kms alias in the model and its generated AWS configuration. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `alias/orders-key`. |

### Relationships

| Relationship           | Kind and multiplicity              | Meaning in the model                                                                                                                                                                                                                                              |
| ---------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `targetKey` → `KmsKey` | reference, [1]; opposite `aliases` | The `targetKey` reference on `KmsAlias` identifies the destination represented by target key. A `KmsKey` can remain independently owned and can participate in other parts of the model. Its opposite `aliases` exposes the same connection from the target side. |

## `GenerateSecretStringConfig`

`GenerateSecretStringConfig` is a configuration record in the AWS platform-specific model. It makes generate secret string config explicit. Its declaration gives the concept a precise home through its declared properties. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                 | Type and multiplicity | What it captures and why it exists                                                                                                                                                       | Accepted values and example                                                                                                                              |
| ------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `secretStringTemplate`    | `String` [1]          | A JSON template merged with the generated key-value pair when the secret must include fixed fields.                                                                                      | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Generate Secret String Config Secret String Template`. |
| `generateStringKey`       | `String` [1]          | The JSON member name under which Secrets Manager places the generated password.                                                                                                          | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `orderId`.                                              |
| `passwordLength`          | `Integer` [1]         | The numeric value used for password length on this generate secret string config. Keeping the quantity in the model lets validation and generation apply the same boundary consistently. | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                                         |
| `excludeCharacters`       | `String` [1]          | Characters removed from the alphabet used for secret generation.                                                                                                                         | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Generate Secret String Config Exclude Characters`.     |
| `excludeLowercase`        | `Boolean` [1]         | The boolean decision for exclude lowercase on this generate secret string config. It keeps an important design choice explicit for review and transformation.                            | Either `true` or `false`. Example: `false`.                                                                                                              |
| `excludeUppercase`        | `Boolean` [1]         | The boolean decision for exclude uppercase on this generate secret string config. It keeps an important design choice explicit for review and transformation.                            | Either `true` or `false`. Example: `false`.                                                                                                              |
| `excludeNumbers`          | `Boolean` [1]         | The boolean decision for exclude numbers on this generate secret string config. It keeps an important design choice explicit for review and transformation.                              | Either `true` or `false`. Example: `false`.                                                                                                              |
| `excludePunctuation`      | `Boolean` [1]         | The boolean decision for exclude punctuation on this generate secret string config. It keeps an important design choice explicit for review and transformation.                          | Either `true` or `false`. Example: `false`.                                                                                                              |
| `includeSpace`            | `Boolean` [1]         | The boolean decision for include space on this generate secret string config. It keeps an important design choice explicit for review and transformation.                                | Either `true` or `false`. Example: `false`.                                                                                                              |
| `requireEachIncludedType` | `Boolean` [1]         | Whether a generated value must contain at least one character from every enabled character category.                                                                                     | Either `true` or `false`. Example: `false`.                                                                                                              |

### Relationships

This class declares no direct relationships.

## `SecretsManagerSecret`

An AWS Secrets Manager secret resource. It provides the provider-specific home for protected configuration, generated values, KMS choice, rotation, replicas, and resource policy.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                  | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                             | Accepted values and example                                                                                                     |
| -------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `secretName`               | `String` [1]          | The secret name that identifies this secrets manager secret in the model and its generated AWS configuration. Transformation role: ETL rule `Secret2SecretsManagerSecret` in `mde/transformations/pim-to-awspsm/workflow-security-config.etl` assigns or materializes this feature while refining `SecretsManagerSecret`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `SecretsManagerSecretExample`. |
| `descriptionText`          | `String` [1]          | The provider-facing descriptive text emitted for this resource. It gives an operator context in the generated template and console without carrying runtime behavior. Within `SecretsManagerSecret`, it applies to this specific element and its role in the surrounding model.                                                                                                                                                                | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Orders processing`.           |
| `generateSecretStringJson` | `String` [1]          | The serialized representation of generate secret string on this secrets manager secret. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                             | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.            |
| `rotationRequired`         | `Boolean` [1]         | Whether this secrets manager secret requires rotation. The flag records an obligation that validation and generation can carry forward.                                                                                                                                                                                                                                                                                                        | Either `true` or `false`. Example: `true`.                                                                                      |
| `replicaRegionsJson`       | `String` [1]          | The serialized representation of replica regions on this secrets manager secret. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                                    | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.            |

### Relationships

| Relationship                                          | Kind and multiplicity             | Meaning in the model                                                                                                                                                                                                                                                                                                                 |
| ----------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `secretValue` → `ValueExpression`                     | containment, [?]                  | The value expression used when a secret has an explicit modelled value.                                                                                                                                                                                                                                                              |
| `generateSecretString` → `GenerateSecretStringConfig` | containment, [?]                  | The structured password-generation policy for this secret.                                                                                                                                                                                                                                                                           |
| `kmsKey` → `KmsKey`                                   | reference, [?]                    | The `kmsKey` reference on `SecretsManagerSecret` selects the KMS key used for provider-side encryption. A `KmsKey` can remain independently owned and can participate in other parts of the model.                                                                                                                                   |
| `rotationSchedule` → `SecretRotationSchedule`         | reference, [?]; opposite `secret` | The `rotationSchedule` reference on `SecretsManagerSecret` attaches the secret rotation behavior to the secret. A `SecretRotationSchedule` can remain independently owned and can participate in other parts of the model. Its opposite `secret` exposes the same connection from the target side.                                   |
| `resourcePolicy` → `SecretsManagerResourcePolicy`     | reference, [?]; opposite `secret` | The `resourcePolicy` reference on `SecretsManagerSecret` attaches the resource-level policy controlling access to the secret or data store. A `SecretsManagerResourcePolicy` can remain independently owned and can participate in other parts of the model. Its opposite `secret` exposes the same connection from the target side. |

## `SecretRotationRules`

The timing policy for automatic secret rotation, expressed either as an interval or schedule together with an optional rotation-window duration.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                | Type and multiplicity | What it captures and why it exists                                                                                                                                                        | Accepted values and example                                                                                             |
| ------------------------ | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `automaticallyAfterDays` | `Integer` [1]         | The numeric value used for automatically after days on this secret rotation rules. Keeping the quantity in the model lets validation and generation apply the same boundary consistently. | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                        |
| `scheduleExpression`     | `String` [1]          | The expression used to determine schedule for this secret rotation rules. It keeps executable or provider-interpreted logic visible to validation and generation.                         | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `$.status == 'READY'`. |
| `durationHours`          | `Integer` [1]         | The numeric value used for duration hours on this secret rotation rules. Keeping the quantity in the model lets validation and generation apply the same boundary consistently.           | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `30`.                       |

### Relationships

This class declares no direct relationships.

## `SecretRotationSchedule`

The resource that joins a secret to its rotation function and timing rules. This separation allows the secret and its rotation lifecycle to remain explicit resources.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute           | Type and multiplicity | What it captures and why it exists                                                                                                                                           | Accepted values and example                                                                                          |
| ------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `rotationRulesJson` | `String` [1]          | The serialized representation of rotation rules on this secret rotation schedule. It carries provider-specific structure that is intentionally retained as document content. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`. |

### Relationships

| Relationship                            | Kind and multiplicity                       | Meaning in the model                                                                                                                                                                                                                                                                                                       |
| --------------------------------------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `rotationRules` → `SecretRotationRules` | containment, [?]                            | The interval or schedule governing automatic secret rotation.                                                                                                                                                                                                                                                              |
| `secret` → `SecretsManagerSecret`       | reference, [1]; opposite `rotationSchedule` | The `secret` reference on `SecretRotationSchedule` identifies the protected value required by this configuration or credential. A `SecretsManagerSecret` can remain independently owned and can participate in other parts of the model. Its opposite `rotationSchedule` exposes the same connection from the target side. |
| `rotationLambda` → `AwsResource`        | reference, [?]                              | The function or provider resource that performs secret rotation.                                                                                                                                                                                                                                                           |

## `SecretsManagerResourcePolicy`

`SecretsManagerResourcePolicy` is an AWS security or configuration record in the AWS platform-specific model. It carries the provider setting for secrets manager resource policy. Its declaration gives the concept a precise home through resource policy, secret. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute            | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                               | Accepted values and example                                                                                          |
| -------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `resourcePolicyJson` | `String` [1]          | The serialized resource policy attached to this resource. It keeps resource-level access rules distinct from the identity policies of callers. Within `SecretsManagerResourcePolicy`, it applies to this specific element and its role in the surrounding model. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`. |

### Relationships

| Relationship                           | Kind and multiplicity                     | Meaning in the model                                                                                                                                                                                                                                                                                                           |
| -------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `resourcePolicy` → `IamPolicyDocument` | containment, [?]                          | The `resourcePolicy` containment on `SecretsManagerResourcePolicy` attaches the resource-level policy controlling access to the secret or data store. The `IamPolicyDocument` objects are owned by `SecretsManagerResourcePolicy` and remain part of its model subtree.                                                        |
| `secret` → `SecretsManagerSecret`      | reference, [1]; opposite `resourcePolicy` | The `secret` reference on `SecretsManagerResourcePolicy` identifies the protected value required by this configuration or credential. A `SecretsManagerSecret` can remain independently owned and can participate in other parts of the model. Its opposite `resourcePolicy` exposes the same connection from the target side. |

## `SsmParameter`

`SsmParameter` is an AWS security or configuration record in the AWS platform-specific model. It carries the provider setting for ssm parameter. Its declaration gives the concept a precise home through value, kms key. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute         | Type and multiplicity  | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted values and example                                                                                                   |
| ----------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `parameterName`   | `String` [1]           | The parameter name that identifies this ssm parameter in the model and its generated AWS configuration. Semantic validation: `SecureParameterShouldUseKmsKey` (secure parameter should use kms key) in `mde/validation/psm/rules/security.evl` the rule's diagnostic or remediation guidance refers to this feature. Transformation role: ETL rule `SecretConfigParameter2SsmParameter` in `mde/transformations/pim-to-awspsm/workflow-security-config.etl` assigns or materializes this feature while refining `SsmParameter`. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `/orders/prod/database-url`. |
| `dataType`        | `String` [1]           | The Systems Manager interpretation applied to the stored value beyond its basic parameter type.                                                                                                                                                                                                                                                                                                                                                                                                                                 | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `BUSINESS`.                  |
| `allowedPattern`  | `String` [1]           | Stores the validation pattern for allowed pattern on the ssm parameter. The field records input/schema validation as an explicit, reviewable input. Transformation role: ETL rule `SecretConfigParameter2SsmParameter` in `mde/transformations/pim-to-awspsm/workflow-security-config.etl` assigns or materializes this feature while refining `SsmParameter`.                                                                                                                                                                  | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `^[A-Z][A-Za-z0-9_-]*$`.     |
| `descriptionText` | `String` [1]           | The provider-facing descriptive text emitted for this resource. It gives an operator context in the generated template and console without carrying runtime behavior. Within `SsmParameter`, it applies to this specific element and its role in the surrounding model. Transformation role: ETL rule `SecretConfigParameter2SsmParameter` in `mde/transformations/pim-to-awspsm/workflow-security-config.etl` assigns or materializes this feature while refining `SsmParameter`.                                              | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Orders processing`.         |
| `parameterType`   | `ParameterType` [1]    | The controlled value used for parameter type on this ssm parameter. It keeps the distinction explicit in the abstract syntax so later rules can interpret it consistently.                                                                                                                                                                                                                                                                                                                                                      | Exactly one of: `STRING`, `STRING_LIST`, `SECURE_STRING`. Example: `STRING`.                                                  |
| `tier`            | `SsmParameterTier` [1] | The controlled value used for tier on this ssm parameter. It keeps the distinction explicit in the abstract syntax so later rules can interpret it consistently.                                                                                                                                                                                                                                                                                                                                                                | Exactly one of: `STANDARD`, `ADVANCED`, `INTELLIGENT_TIERING`. Example: `STANDARD`.                                           |

### Relationships

| Relationship                | Kind and multiplicity | Meaning in the model                                                                                                                                                                       |
| --------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `value` → `ValueExpression` | containment, [?]      | The structured value expression supplying this setting; it may be literal, parameter-backed, resource-derived, or composite.                                                               |
| `kmsKey` → `KmsKey`         | reference, [?]        | The `kmsKey` reference on `SsmParameter` selects the KMS key used for provider-side encryption. A `KmsKey` can remain independently owned and can participate in other parts of the model. |

## `SsmParameterValueExpression`

`SsmParameterValueExpression` is an AWS security or configuration record in the AWS platform-specific model. It carries the provider setting for ssm parameter value expression. Its declaration gives the concept a precise home through ssm parameter. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `ValueExpression`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

This class declares no attributes of its own. It inherits the attributes of its supertype, if any.

### Relationships

| Relationship                    | Kind and multiplicity | Meaning in the model                                               |
| ------------------------------- | --------------------- | ------------------------------------------------------------------ |
| `ssmParameter` → `SsmParameter` | reference, [?]        | The Systems Manager parameter referenced by this value expression. |

## `SecretValueExpression`

`SecretValueExpression` is a supporting value object in the AWS platform-specific model. It carries secret value expression as explicit model data. Its declaration gives the concept a precise home through secret ref. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `ValueExpression`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

This class declares no attributes of its own. It inherits the attributes of its supertype, if any.

### Relationships

| Relationship                         | Kind and multiplicity | Meaning in the model                                                      |
| ------------------------------------ | --------------------- | ------------------------------------------------------------------------- |
| `secretRef` → `SecretsManagerSecret` | reference, [?]        | The Secrets Manager secret from which this expression resolves its value. |
