# Amazon Cognito identity resources

Cognito classes describe user pools, clients, OAuth, recovery, schema attributes, groups, domains, and identity pools.

Source: `mde/metamodels/psm/awspsm-identity.emf`.

## `CognitoUserPool`

An Amazon Cognito user-pool resource. It models registration, password, MFA, recovery, schema, email, Lambda triggers, and account policy settings for managed user identity.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                    | Type and multiplicity         | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted values and example                                                                                                                                                                     |
| ---------------------------- | ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `userPoolName`               | `String` [1]                  | The user pool name that identifies this cognito user pool in the model and its generated AWS configuration. Transformation role: ETL rule `IdentityProvider2CognitoUserPool` in `mde/transformations/pim-to-awspsm/workflow-security-config.etl` assigns or materializes this feature while refining `CognitoUserPool`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                    | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `CognitoUserPoolExample`.                                                                      |
| `mfaRationale`               | `String` [1]                  | The explanation for choosing mfa on this cognito user pool. It preserves the design reasoning that cannot be recovered from the resulting value alone.                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `The choice protects the business outcome while keeping the design independently deployable.`. |
| `usernameAttributes`         | `String` [*]                  | The attributes a user may present as the pool's sign-in name, rather than relying only on Cognito's internal username.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A collection of values. Example: [`Cognito User Pool Username Attributes`, `Cognito User Pool Username Attributes-2`].                                                                          |
| `aliasAttributes`            | `String` [*]                  | Alternative user attributes Cognito accepts as aliases for sign-in and account lookup. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                                                                                                                                                                                                                                                     | A collection of values. Example: [`Cognito User Pool Alias Attributes`, `Cognito User Pool Alias Attributes-2`].                                                                                |
| `autoVerifiedAttributes`     | `String` [*]                  | The user attributes Cognito verifies automatically and may use for account confirmation and recovery. Transformation role: ETL rule `IdentityProvider2CognitoUserPool` in `mde/transformations/pim-to-awspsm/workflow-security-config.etl` reads or derives this feature while refining `CognitoUserPool`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                                 | A collection of values. Example: [`Cognito User Pool Auto Verified Attributes`, `Cognito User Pool Auto Verified Attributes-2`].                                                                |
| `accountRecoverySettingJson` | `String` [1]                  | The serialized representation of account recovery setting on this cognito user pool. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                                                                                                                                                                                                 | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.                                                                            |
| `adminCreateUserConfigJson`  | `String` [1]                  | The serialized representation of admin create user config on this cognito user pool. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                                                                                                                                                                                                 | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.                                                                            |
| `emailConfigurationJson`     | `String` [1]                  | The serialized representation of email configuration on this cognito user pool. It carries provider-specific structure that is intentionally retained as document content. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                                                                                                                                                                 | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.                                                                            |
| `smsConfigurationJson`       | `String` [1]                  | Records serialized JSON representation of sms configuration for cognito user pool. It keeps preserving structured provider or contract detail explicit during review and transformation, so later steps do not have to infer it. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                                                                                                           | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.                                                                            |
| `lambdaConfigJson`           | `String` [1]                  | Records serialized JSON representation of lambda config for cognito user pool. It keeps preserving structured provider or contract detail explicit during review and transformation, so later steps do not have to infer it. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                                                                                                                                                               | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.                                                                            |
| `policiesJson`               | `String` [1]                  | The serialized representation of policies on this cognito user pool. It carries provider-specific structure that is intentionally retained as document content. Transformation role: ETL rule `IdentityProvider2CognitoUserPool` in `mde/transformations/pim-to-awspsm/workflow-security-config.etl` assigns or materializes this feature while refining `CognitoUserPool`. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                                                                | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.                                                                            |
| `schemaJson`                 | `String` [1]                  | The serialized representation of schema on this cognito user pool. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                                                                                                                                                                                                                   | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.                                                                            |
| `deletionProtection`         | `Boolean` [1]                 | The boolean decision for deletion protection on this cognito user pool. It keeps an important design choice explicit for review and transformation.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Either `true` or `false`. Example: `false`.                                                                                                                                                     |
| `mfaConfiguration`           | `CognitoMfaConfiguration` [1] | The controlled value used for mfa configuration on this cognito user pool. It keeps the distinction explicit in the abstract syntax so later rules can interpret it consistently.                                                                                                                                                                                                                                                                                                                                                                                                                               | Exactly one of: `OFF`, `ON`, `OPTIONAL`. Example: `OFF`.                                                                                                                                        |
| `mfaDecision`                | `Decision` [1]                | The controlled value used for mfa decision on this cognito user pool. It keeps the distinction explicit in the abstract syntax so later rules can interpret it consistently. Semantic validation: `MfaDecisionMadeForProductionCriticalPools` (mfa decision made for production critical pools) in `mde/validation/psm/rules/identity.evl` the related value or object must be explicitly provided. Transformation role: ETL rule `IdentityProvider2CognitoUserPool` in `mde/transformations/pim-to-awspsm/workflow-security-config.etl` assigns or materializes this feature while refining `CognitoUserPool`. | Exactly one of: `UNDECIDED`, `REQUIRED`, `NOT_REQUIRED`, `ACCEPTED`, `NEEDS_REVIEW`, `GENERATOR_OWNED`. Example: `UNDECIDED`.                                                                   |

### Relationships

| Relationship                                               | Kind and multiplicity               | Meaning in the model                                                                                                                                                                                                                         |
| ---------------------------------------------------------- | ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `accountRecoverySetting` → `CognitoAccountRecoverySetting` | containment, [?]                    | The optional ordered recovery policy used when a user cannot access the account.                                                                                                                                                             |
| `emailConfiguration` → `CognitoEmailConfiguration`         | containment, [?]                    | The `emailConfiguration` containment on `CognitoUserPool` attaches the configuration record represented by email configuration. The `CognitoEmailConfiguration` objects are owned by `CognitoUserPool` and remain part of its model subtree. |
| `lambdaConfig` → `CognitoLambdaConfig`                     | containment, [?]                    | The `lambdaConfig` containment on `CognitoUserPool` attaches the configuration record represented by lambda config. The `CognitoLambdaConfig` objects are owned by `CognitoUserPool` and remain part of its model subtree.                   |
| `passwordPolicy` → `CognitoPasswordPolicy`                 | containment, [?]                    | The `passwordPolicy` containment on `CognitoUserPool` attaches the access or governance decision represented by password policy. The `CognitoPasswordPolicy` objects are owned by `CognitoUserPool` and remain part of its model subtree.    |
| `schemaAttributes` → `CognitoSchemaAttribute`              | containment, [*]                    | The `schemaAttributes` containment on `CognitoUserPool` connects this element to the data shape represented by schema attributes. The `CognitoSchemaAttribute` objects are owned by `CognitoUserPool` and remain part of its model subtree.  |
| `clients` → `CognitoUserPoolClient`                        | reference, [*]; opposite `userPool` | The application clients registered against this user pool.                                                                                                                                                                                   |
| `groups` → `CognitoUserPoolGroup`                          | reference, [*]; opposite `userPool` | User-pool groups defined for authorization or membership organisation.                                                                                                                                                                       |
| `domain` → `CognitoUserPoolDomain`                         | reference, [?]; opposite `userPool` | The hosted or custom domain associated with the user pool.                                                                                                                                                                                   |

## `CognitoPasswordPolicy`

`CognitoPasswordPolicy` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito password policy. Its declaration gives the concept a precise home through its declared properties. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                       | Type and multiplicity | What it captures and why it exists                                                                                                                                                                  | Accepted values and example                                                                      |
| ------------------------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `minimumLength`                 | `Integer` [1]         | The numeric value used for minimum length on this cognito password policy. Keeping the quantity in the model lets validation and generation apply the same boundary consistently.                   | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`. |
| `requireUppercase`              | `Boolean` [1]         | The boolean decision for require uppercase on this cognito password policy. It keeps an important design choice explicit for review and transformation.                                             | Either `true` or `false`. Example: `false`.                                                      |
| `requireLowercase`              | `Boolean` [1]         | The boolean decision for require lowercase on this cognito password policy. It keeps an important design choice explicit for review and transformation.                                             | Either `true` or `false`. Example: `false`.                                                      |
| `requireNumbers`                | `Boolean` [1]         | The boolean decision for require numbers on this cognito password policy. It keeps an important design choice explicit for review and transformation.                                               | Either `true` or `false`. Example: `false`.                                                      |
| `requireSymbols`                | `Boolean` [1]         | The boolean decision for require symbols on this cognito password policy. It keeps an important design choice explicit for review and transformation.                                               | Either `true` or `false`. Example: `false`.                                                      |
| `temporaryPasswordValidityDays` | `Integer` [1]         | The numeric value used for temporary password validity days on this cognito password policy. Keeping the quantity in the model lets validation and generation apply the same boundary consistently. | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`. |

### Relationships

This class declares no direct relationships.

## `CognitoSchemaAttribute`

`CognitoSchemaAttribute` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito schema attribute. Its declaration gives the concept a precise home through its declared properties. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                     | Accepted values and example                                                                                                                           |
| ------------------------ | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `attributeName`          | `String` [1]          | The attribute name that identifies this cognito schema attribute in the model and its generated AWS configuration.                                                                                                                                                     | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `CognitoSchemaAttributeExample`.                     |
| `attributeDataType`      | `String` [1]          | The Cognito schema data type used to interpret and constrain values of this user attribute.                                                                                                                                                                            | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `BUSINESS`.                                          |
| `required`               | `Boolean` [1]         | Whether the modeled value or participation is required. It records a business or architectural obligation that would otherwise be inferred from context. Within `CognitoSchemaAttribute`, it applies to this specific element and its role in the surrounding model.   | Either `true` or `false`. Example: `true`.                                                                                                            |
| `mutable`                | `Boolean` [1]         | The boolean decision for mutable on this cognito schema attribute. It keeps an important design choice explicit for review and transformation. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/naming.eol`. | Either `true` or `false`. Example: `false`.                                                                                                           |
| `developerOnlyAttribute` | `String` [1]          | Whether this schema member is reserved for privileged server-side access rather than ordinary user read and write operations.                                                                                                                                          | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Cognito Schema Attribute Developer Only Attribute`. |
| `minLength`              | `String` [1]          | The minimum permitted character length for a string-valued schema attribute.                                                                                                                                                                                           | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Cognito Schema Attribute Min Length`.               |
| `maxLength`              | `String` [1]          | The maximum permitted character length for a string-valued schema attribute.                                                                                                                                                                                           | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Cognito Schema Attribute Max Length`.               |
| `minimum`                | `String` [1]          | The lower bound for a numeric schema attribute, represented in the textual form accepted by Cognito.                                                                                                                                                                   | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Cognito Schema Attribute Minimum`.                  |
| `maximum`                | `String` [1]          | The upper bound for a numeric schema attribute, represented in the textual form accepted by Cognito.                                                                                                                                                                   | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Cognito Schema Attribute Maximum`.                  |

### Relationships

This class declares no direct relationships.

## `CognitoEmailConfiguration`

`CognitoEmailConfiguration` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito email configuration. Its declaration gives the concept a precise home through its declared properties. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute             | Type and multiplicity | What it captures and why it exists                                                                                              | Accepted values and example                                                                                                                               |
| --------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `emailSendingAccount` | `String` [1]          | The Cognito email-delivery mode, deciding whether the pool uses its default service integration or the configured SES identity. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `operator@example.com`.                                  |
| `from`                | `String` [1]          | The sender identity displayed on email sent by the user pool.                                                                   | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Cognito Email Configuration From`.                      |
| `replyToEmailAddress` | `String` [1]          | The address to which replies to user-pool email are directed.                                                                   | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `operator@example.com`.                                  |
| `sourceArn`           | `String` [1]          | The verified Amazon SES identity ARN used as the source of user-pool email.                                                     | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `arn:aws:lambda:us-east-1:123456789012:function:orders`. |
| `configurationSet`    | `String` [1]          | The Amazon SES configuration set applied to messages sent for the user pool.                                                    | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Cognito Email Configuration Configuration Set`.         |

### Relationships

This class declares no direct relationships.

## `CognitoAccountRecoverySetting`

`CognitoAccountRecoverySetting` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito account recovery setting. Its declaration gives the concept a precise home through recovery mechanisms. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

This class declares no attributes of its own. It inherits the attributes of its supertype, if any.

### Relationships

| Relationship                                      | Kind and multiplicity | Meaning in the model                                                      |
| ------------------------------------------------- | --------------------- | ------------------------------------------------------------------------- |
| `recoveryMechanisms` → `CognitoRecoveryMechanism` | containment, [*]      | The ordered mechanisms Cognito may use to help a user recover an account. |

## `CognitoRecoveryMechanism`

`CognitoRecoveryMechanism` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito recovery mechanism. Its declaration gives the concept a precise home through its declared properties. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute  | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted values and example                                                                                                         |
| ---------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| `name`     | `String` [1]          | The model name used when people and transformations refer to this element. It is the readable identity within the surrounding model, rather than a provider-generated physical name. Within `CognitoRecoveryMechanism`, it applies to this specific element and its role in the surrounding model. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/contracts.eol`. The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/naming.eol`. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `CognitoRecoveryMechanismExample`. |
| `priority` | `Integer` [1]         | The relative urgency of this element in business or operational review. It helps order attention while leaving the element's meaning unchanged. Within `CognitoRecoveryMechanism`, it applies to this specific element and its role in the surrounding model.                                                                                                                                                                                                                                                                        | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                    |

### Relationships

This class declares no direct relationships.

## `CognitoLambdaConfig`

`CognitoLambdaConfig` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito lambda config. Its declaration gives the concept a precise home through pre sign up, post confirmation, pre authentication. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

This class declares no attributes of its own. It inherits the attributes of its supertype, if any.

### Relationships

| Relationship                                  | Kind and multiplicity | Meaning in the model                                                                                                                                                                                                                      |
| --------------------------------------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `preSignUp` → `AwsResource`                   | reference, [?]        | The resource invoked before Cognito accepts a new registration.                                                                                                                                                                           |
| `postConfirmation` → `AwsResource`            | reference, [?]        | The resource invoked after a user confirms registration.                                                                                                                                                                                  |
| `preAuthentication` → `AwsResource`           | reference, [?]        | The resource invoked immediately before Cognito authenticates a user.                                                                                                                                                                     |
| `postAuthentication` → `AwsResource`          | reference, [?]        | The resource invoked after successful authentication.                                                                                                                                                                                     |
| `customMessage` → `AwsResource`               | reference, [?]        | The `customMessage` reference on `CognitoLambdaConfig` connects this element to the event or message path represented by custom message. An `AwsResource` can remain independently owned and can participate in other parts of the model. |
| `defineAuthChallenge` → `AwsResource`         | reference, [?]        | The resource that determines the next step in a custom authentication challenge.                                                                                                                                                          |
| `createAuthChallenge` → `AwsResource`         | reference, [?]        | The resource that creates the current custom authentication challenge.                                                                                                                                                                    |
| `verifyAuthChallengeResponse` → `AwsResource` | reference, [?]        | The resource that checks the user's custom challenge response.                                                                                                                                                                            |
| `preTokenGeneration` → `AwsResource`          | reference, [?]        | The resource allowed to adjust claims immediately before Cognito issues tokens.                                                                                                                                                           |
| `userMigration` → `AwsResource`               | reference, [?]        | The resource used to locate or migrate a user who is absent from this pool.                                                                                                                                                               |

## `CognitoUserPoolClient`

`CognitoUserPoolClient` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito user pool client. Its declaration gives the concept a precise home through oauth configuration, user pool. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                         | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                        | Accepted values and example                                                                                                                                |
| --------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `clientName`                      | `String` [1]          | The client name that identifies this cognito user pool client in the model and its generated AWS configuration.                                                                                                                                                           | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `CognitoUserPoolClientExample`.                           |
| `generateSecret`                  | `Boolean` [1]         | The boolean decision for generate secret on this cognito user pool client. It keeps an important design choice explicit for review and transformation.                                                                                                                    | Either `true` or `false`. Example: `false`.                                                                                                                |
| `explicitAuthFlows`               | `String` [*]          | The Cognito authentication flows that this application client is allowed to initiate. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                | A collection of values. Example: [`Cognito User Pool Client Explicit Auth Flows`, `Cognito User Pool Client Explicit Auth Flows-2`].                       |
| `supportedIdentityProviders`      | `String` [*]          | The identity providers through which this application client permits sign-in.                                                                                                                                                                                             | A collection of values. Example: [`Cognito User Pool Client Supported Identity Providers`, `Cognito User Pool Client Supported Identity Providers-2`].     |
| `callbackUrls`                    | `String` [*]          | The redirect URIs to which Cognito may return a browser after successful authorization. Semantic validation: `OAuthClientHasCallbackUrls` (oauth client has callback urls) in `mde/validation/psm/rules/identity.evl` the collection or referenced set must not be empty. | A collection of values. Example: [`Cognito User Pool Client Callback Urls`, `Cognito User Pool Client Callback Urls-2`].                                   |
| `logoutUrls`                      | `String` [*]          | The redirect URIs accepted after a user signs out through the Cognito hosted interface.                                                                                                                                                                                   | A collection of values. Example: [`Cognito User Pool Client Logout Urls`, `Cognito User Pool Client Logout Urls-2`].                                       |
| `allowedOAuthFlows`               | `String` [*]          | The OAuth grant flows enabled for this application client.                                                                                                                                                                                                                | A collection of values. Example: [`Cognito User Pool Client Allowed Oauth Flows`, `Cognito User Pool Client Allowed Oauth Flows-2`].                       |
| `allowedOAuthScopes`              | `String` [*]          | The OAuth scopes the client may request during authorization.                                                                                                                                                                                                             | A collection of values. Example: [`orders`, `orders-2`].                                                                                                   |
| `allowedOAuthFlowsUserPoolClient` | `Boolean` [1]         | The boolean decision for allowed o auth flows user pool client on this cognito user pool client. It keeps an important design choice explicit for review and transformation.                                                                                              | Either `true` or `false`. Example: `true`.                                                                                                                 |
| `accessTokenValidity`             | `Integer` [1]         | The numeric value used for access token validity on this cognito user pool client. Keeping the quantity in the model lets validation and generation apply the same boundary consistently.                                                                                 | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                                           |
| `idTokenValidity`                 | `Integer` [1]         | The numeric value used for id token validity on this cognito user pool client. Keeping the quantity in the model lets validation and generation apply the same boundary consistently.                                                                                     | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                                           |
| `refreshTokenValidity`            | `Integer` [1]         | The period for which refresh tokens issued to this client remain usable. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`.                                                                             | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                                           |
| `tokenValidityUnitsJson`          | `String` [1]          | The serialized representation of token validity units on this cognito user pool client. It carries provider-specific structure that is intentionally retained as document content.                                                                                        | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.                                       |
| `preventUserExistenceErrors`      | `String` [1]          | The Cognito response mode used to avoid disclosing whether a supplied username exists.                                                                                                                                                                                    | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Cognito User Pool Client Prevent User Existence Errors`. |

### Relationships

| Relationship                                       | Kind and multiplicity              | Meaning in the model                                                                                                                                                                                                                                     |
| -------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `oauthConfiguration` → `CognitoOAuthConfiguration` | containment, [?]                   | The `oauthConfiguration` containment on `CognitoUserPoolClient` attaches the configuration record represented by oauth configuration. The `CognitoOAuthConfiguration` objects are owned by `CognitoUserPoolClient` and remain part of its model subtree. |
| `userPool` → `CognitoUserPool`                     | reference, [1]; opposite `clients` | The Cognito user pool to which this client, group, domain, or identity configuration belongs.                                                                                                                                                            |

## `CognitoOAuthConfiguration`

`CognitoOAuthConfiguration` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito o auth configuration. Its declaration gives the concept a precise home through its declared properties. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `TraceableElement`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                         | Type and multiplicity | What it captures and why it exists                                                                                                                                               | Accepted values and example                                                                                                                                  |
| --------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `callbackUrls`                    | `String` [*]          | The redirect URIs allowed after the OAuth authorization exchange completes.                                                                                                      | A collection of values. Example: [`Cognito Oauth Configuration Callback Urls`, `Cognito Oauth Configuration Callback Urls-2`].                               |
| `logoutUrls`                      | `String` [*]          | The destinations Cognito may use after ending an OAuth-backed session.                                                                                                           | A collection of values. Example: [`Cognito Oauth Configuration Logout Urls`, `Cognito Oauth Configuration Logout Urls-2`].                                   |
| `allowedOAuthFlows`               | `String` [*]          | The OAuth grant types enabled by this reusable client configuration.                                                                                                             | A collection of values. Example: [`Cognito Oauth Configuration Allowed Oauth Flows`, `Cognito Oauth Configuration Allowed Oauth Flows-2`].                   |
| `allowedOAuthScopes`              | `String` [*]          | The scopes that an authorization request may obtain through this configuration.                                                                                                  | A collection of values. Example: [`orders`, `orders-2`].                                                                                                     |
| `allowedOAuthFlowsUserPoolClient` | `Boolean` [1]         | The boolean decision for allowed o auth flows user pool client on this cognito o auth configuration. It keeps an important design choice explicit for review and transformation. | Either `true` or `false`. Example: `true`.                                                                                                                   |
| `supportedIdentityProviders`      | `String` [*]          | The upstream identity providers available to the OAuth client.                                                                                                                   | A collection of values. Example: [`Cognito Oauth Configuration Supported Identity Providers`, `Cognito Oauth Configuration Supported Identity Providers-2`]. |

### Relationships

This class declares no direct relationships.

## `CognitoUserPoolGroup`

`CognitoUserPoolGroup` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito user pool group. Its declaration gives the concept a precise home through user pool, role. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute         | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                              | Accepted values and example                                                                                                     |
| ----------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `groupName`       | `String` [1]          | The group name that identifies this cognito user pool group in the model and its generated AWS configuration.                                                                                                                                                                   | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `CognitoUserPoolGroupExample`. |
| `descriptionText` | `String` [1]          | The provider-facing descriptive text emitted for this resource. It gives an operator context in the generated template and console without carrying runtime behavior. Within `CognitoUserPoolGroup`, it applies to this specific element and its role in the surrounding model. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `Orders processing`.           |
| `precedence`      | `Integer` [1]         | The numeric value used for precedence on this cognito user pool group. Keeping the quantity in the model lets validation and generation apply the same boundary consistently.                                                                                                   | A numeric `Integer` value; use the unit or boundary documented for this attribute. Example: `1`.                                |

### Relationships

| Relationship                   | Kind and multiplicity             | Meaning in the model                                                                                                                                                                                                  |
| ------------------------------ | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `userPool` → `CognitoUserPool` | reference, [1]; opposite `groups` | The Cognito user pool to which this client, group, domain, or identity configuration belongs.                                                                                                                         |
| `role` → `IamRole`             | reference, [?]                    | The `role` reference on `CognitoUserPoolGroup` attaches the execution role that grants the resource its AWS permissions. An `IamRole` can remain independently owned and can participate in other parts of the model. |

## `CognitoUserPoolDomain`

`CognitoUserPoolDomain` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito user pool domain. Its declaration gives the concept a precise home through user pool. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute        | Type and multiplicity | What it captures and why it exists                                            | Accepted values and example                                                                                                                               |
| ---------------- | --------------------- | ----------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `domain`         | `String` [1]          | The hosted UI domain prefix or custom domain assigned to the user pool.       | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `orders.example.com`.                                    |
| `certificateArn` | `String` [1]          | The ACM certificate ARN used when `domain` denotes a custom user-pool domain. | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `arn:aws:lambda:us-east-1:123456789012:function:orders`. |

### Relationships

| Relationship                   | Kind and multiplicity             | Meaning in the model                                                                          |
| ------------------------------ | --------------------------------- | --------------------------------------------------------------------------------------------- |
| `userPool` → `CognitoUserPool` | reference, [1]; opposite `domain` | The Cognito user pool to which this client, group, domain, or identity configuration belongs. |

## `CognitoIdentityPool`

`CognitoIdentityPool` is a Cognito deployment record in the AWS platform-specific model. It carries the identity settings for cognito identity pool. Its declaration gives the concept a precise home through its declared properties. Its references provide the wiring that lets generation assemble the corresponding AWS design.

Direct supertypes: `AwsResource`. Inherited attributes and marker capabilities are documented in the [shared kernel](../shared-kernel.md); this section lists every attribute declared by this class.

### Declared attributes

| Attribute                        | Type and multiplicity | What it captures and why it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted values and example                                                                                                    |
| -------------------------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `identityPoolName`               | `String` [1]          | The identity pool name that identifies this cognito identity pool in the model and its generated AWS configuration.                                                                                                                                                                                                                                                                                                                                                                                   | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `CognitoIdentityPoolExample`. |
| `allowUnauthenticatedIdentities` | `Boolean` [1]         | The boolean decision for allow unauthenticated identities on this cognito identity pool. It keeps an important design choice explicit for review and transformation. Semantic validation: `UnauthenticatedIdentitiesRequireReview` (unauthenticated identities require review) in `mde/validation/psm/rules/identity.evl` the value must satisfy a numeric or ordering boundary. Generation role: The artifact generator references this feature in `mde/generation/awspsm-to-artifacts/lib/cfn.eol`. | Either `true` or `false`. Example: `true`.                                                                                     |
| `cognitoIdentityProvidersJson`   | `String` [1]          | The serialized representation of cognito identity providers on this cognito identity pool. It carries provider-specific structure that is intentionally retained as document content.                                                                                                                                                                                                                                                                                                                 | A free-form `String`, subject to this class's semantic meaning and any EVL constraints. Example: `{"enabled":true}`.           |

### Relationships

This class declares no direct relationships.
