openapi: 3.1.0
info:
  title: Varka Backend API
  version: 0.0.1
  description: REST API for authentication, projects, CIM/PIM/PSM models, transformations, artifacts, modeling metadata, and diagram layout.
  license:
    name: MIT
    identifier: MIT
servers:
  - url: http://127.0.0.1:8080
    description: Local development
security:
  - AuthToken: []
tags:
  - name: Health
    description: Backend status checks.
  - name: Auth
    description: User registration, login, session, and profile endpoints.
  - name: Projects
    description: Project records, active models, and membership.
  - name: Models
    description: CIM, PIM, and PSM model CRUD, validation, import, and export.
  - name: Transformations
    description: MDE transformations and job tracking.
  - name: Artifacts
    description: Generated artifact records, files, and downloads.
  - name: Modeling
    description: Modeling UI configuration and layout services.
  - name: Assistant
    description: Durable AI modeling assistant sessions, turns, checkpoints, and SSE replay.
  - name: Future
    description: Planned feature placeholders that currently return 501.
paths:
  /api/health:
    get:
      tags: [Health]
      summary: Get backend health
      security: []
      responses:
        "200":
          description: Backend health status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /api/auth/register:
    post:
      tags: [Auth]
      summary: Register a user
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "200":
          description: Created auth session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/Conflict"
  /api/auth/login:
    post:
      tags: [Auth]
      summary: Create an auth session
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Created auth session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/auth/guest:
    post:
      tags: [Auth]
      summary: Create an anonymous guest session
      description: Creates an isolated guest account. Guest accounts are limited to five assistant prompts by default.
      security: []
      responses:
        "200":
          description: Created guest session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
  /api/auth/me:
    get:
      tags: [Auth]
      summary: Get current user
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserDto"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      tags: [Auth]
      summary: Update current user profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMeRequest"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserDto"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/auth/logout:
    post:
      tags: [Auth]
      summary: End the current auth session
      responses:
        "200":
          description: Session ended
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/projects:
    get:
      tags: [Projects]
      summary: List projects visible to the current user
      responses:
        "200":
          description: Projects
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectRecord"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Projects]
      summary: Create a project
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveProjectRequest"
      responses:
        "200":
          description: Created project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectRecord"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/projects/{id}:
    parameters:
      - $ref: "#/components/parameters/ProjectId"
    get:
      tags: [Projects]
      summary: Get a project
      responses:
        "200":
          description: Project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectRecord"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [Projects]
      summary: Update a project
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveProjectRequest"
      responses:
        "200":
          description: Updated project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectRecord"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    delete:
      tags: [Projects]
      summary: Delete a project
      responses:
        "200":
          description: Project deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/projects/{id}/download:
    parameters:
      - $ref: "#/components/parameters/ProjectId"
    get:
      tags: [Projects]
      summary: Download project ZIP
      responses:
        "200":
          description: Project ZIP archive
          headers:
            Content-Disposition:
              schema:
                type: string
          content:
            application/zip:
              schema:
                type: string
                format: binary
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/projects/{id}/members:
    parameters:
      - $ref: "#/components/parameters/ProjectId"
    get:
      tags: [Projects]
      summary: List project members
      responses:
        "200":
          description: Project members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectMember"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/projects/{id}/invite:
    parameters:
      - $ref: "#/components/parameters/ProjectId"
    post:
      tags: [Projects]
      summary: Invite an existing user to a project
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InviteRequest"
      responses:
        "200":
          description: Added member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMember"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/projects/{id}/members/{userId}:
    parameters:
      - $ref: "#/components/parameters/ProjectId"
      - name: userId
        in: path
        required: true
        schema:
          type: string
    put:
      tags: [Projects]
      summary: Update a project member role
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMemberRoleRequest"
      responses:
        "200":
          description: Updated member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMember"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Projects]
      summary: Revoke project access
      responses:
        "200":
          description: Access revoked
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/{level}:
    parameters:
      - $ref: "#/components/parameters/ModelLevelPath"
    get:
      tags: [Models]
      summary: List CIM, PIM, or PSM model summaries
      parameters:
        - name: projectId
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Model summaries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ModelSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Models]
      summary: Create a CIM, PIM, or PSM model
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveModelRequest"
      responses:
        "200":
          description: Created model summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModelSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/{level}/{id}:
    parameters:
      - $ref: "#/components/parameters/ModelLevelPath"
      - $ref: "#/components/parameters/ModelId"
    get:
      tags: [Models]
      summary: Get a stored model
      responses:
        "200":
          description: Model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModelRecord"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [Models]
      summary: Replace a stored model
      description: Requires `expectedRevision`; returns 409 if the model has changed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveModelRequest"
      responses:
        "200":
          description: Updated model summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModelSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    patch:
      tags: [Models]
      summary: Patch a stored model
      description: Supports JSON Pointer `add`, `replace`, and `remove` operations. Root replacement is not supported.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatchModelRequest"
      responses:
        "200":
          description: Updated model summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModelSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    delete:
      tags: [Models]
      summary: Delete a stored model
      responses:
        "200":
          description: Model deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/{level}/validate:
    parameters:
      - $ref: "#/components/parameters/ModelLevelPath"
    post:
      tags: [Models]
      summary: Validate unsaved model JSON
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveModelRequest"
      responses:
        "200":
          description: Validation result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/{level}/{id}/validate:
    parameters:
      - $ref: "#/components/parameters/ModelLevelPath"
      - $ref: "#/components/parameters/ModelId"
    post:
      tags: [Models]
      summary: Validate a stored model
      responses:
        "200":
          description: Validation result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/{level}/{id}/validate/jobs:
    parameters:
      - $ref: "#/components/parameters/ModelLevelPath"
      - $ref: "#/components/parameters/ModelId"
    post:
      tags: [Models]
      summary: Submit asynchronous validation for a stored model
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                expectedRevision:
                  type: integer
                  format: int64
      responses:
        "202":
          $ref: "#/components/responses/MdeJobAccepted"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /api/{level}/export:
    parameters:
      - $ref: "#/components/parameters/ModelLevelPath"
    post:
      tags: [Models]
      summary: Export unsaved model JSON
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExportRequest"
      responses:
        "200":
          description: Exported model file
          headers:
            Content-Disposition:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
            application/xml:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/{level}/{id}/export:
    parameters:
      - $ref: "#/components/parameters/ModelLevelPath"
      - $ref: "#/components/parameters/ModelId"
    post:
      tags: [Models]
      summary: Export a stored model
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExportRequest"
      responses:
        "200":
          description: Exported model file
          headers:
            Content-Disposition:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
            application/xml:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/{level}/import:
    parameters:
      - $ref: "#/components/parameters/ModelLevelPath"
    post:
      tags: [Models]
      summary: Import model JSON or XMI
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [projectId, file]
              properties:
                projectId:
                  type: string
                format:
                  type: string
                  enum: [json, xmi]
                  default: json
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Imported model payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
  /api/transformations/cim-to-pim:
    post:
      tags: [Transformations]
      summary: Generate a PIM model from a CIM model
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        $ref: "#/components/requestBodies/TransformRequest"
      responses:
        "202":
          $ref: "#/components/responses/MdeJobAccepted"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
  /api/transformations/pim-to-psm:
    post:
      tags: [Transformations]
      summary: Generate a PSM model from a PIM model
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        $ref: "#/components/requestBodies/TransformRequest"
      responses:
        "202":
          $ref: "#/components/responses/MdeJobAccepted"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
  /api/transformations/psm-to-artifact:
    post:
      tags: [Transformations]
      summary: Generate deployable artifacts from a PSM model
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        $ref: "#/components/requestBodies/TransformRequest"
      responses:
        "202":
          $ref: "#/components/responses/MdeJobAccepted"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
  /api/transformations/jobs/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Transformations]
      summary: Get a transformation job
      responses:
        "200":
          description: Job record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MdeJobRecord"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/transformations/jobs/{id}/cancel:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Transformations]
      summary: Cancel a transformation job
      responses:
        "200":
          description: Cancelled job record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MdeJobRecord"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/artifact:
    get:
      tags: [Artifacts]
      summary: List artifacts for a project
      parameters:
        - name: projectId
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Artifacts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ArtifactRecord"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/artifact/{id}:
    parameters:
      - $ref: "#/components/parameters/ArtifactId"
    get:
      tags: [Artifacts]
      summary: Get an artifact
      responses:
        "200":
          description: Artifact
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactRecord"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/artifact/{id}/file:
    parameters:
      - $ref: "#/components/parameters/ArtifactId"
      - name: path
        in: query
        required: true
        schema:
          type: string
    get:
      tags: [Artifacts]
      summary: Read an artifact file
      responses:
        "200":
          description: File contents
          content:
            text/plain:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/artifact/{id}/files:
    parameters:
      - $ref: "#/components/parameters/ArtifactId"
    put:
      tags: [Artifacts]
      summary: Save or replace an artifact file
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveFileRequest"
      responses:
        "200":
          description: Updated artifact
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactRecord"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/artifact/{id}/download:
    parameters:
      - $ref: "#/components/parameters/ArtifactId"
    get:
      tags: [Artifacts]
      summary: Download an artifact as a ZIP archive
      responses:
        "200":
          description: ZIP archive
          headers:
            Content-Disposition:
              schema:
                type: string
          content:
            application/zip:
              schema:
                type: string
                format: binary
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/modeling/config:
    get:
      tags: [Modeling]
      summary: Get modeling palette and UI metadata
      security: []
      responses:
        "200":
          description: Modeling configuration
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
  /api/modeling/process/{level}:
    get:
      tags: [Modeling]
      summary: Get modeling methodology process for a level
      security: []
      parameters:
        - name: level
          in: path
          required: true
          schema:
            type: string
            enum: [cim, pim, psm]
      responses:
        "200":
          description: Modeling process definition
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
  /api/modeling/process/{level}/coverage:
    get:
      tags: [Modeling]
      summary: Get metamodel coverage for a modeling process level
      security: []
      parameters:
        - name: level
          in: path
          required: true
          schema:
            type: string
            enum: [cim, pim, psm]
      responses:
        "200":
          description: Modeling process coverage
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
  /api/layout:
    post:
      tags: [Modeling]
      summary: Auto-layout diagram nodes and edges
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LayoutRequest"
      responses:
        "200":
          description: Layout result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LayoutResponse"
  /api/{level}/{modelId}/views/{viewId}/layout:
    post:
      tags: [Modeling]
      summary: Lazily auto-layout and persist one stored model view
      parameters:
        - name: level
          in: path
          required: true
          schema:
            type: string
            enum: [cim, pim, psm]
        - name: modelId
          in: path
          required: true
          schema:
            type: string
        - name: viewId
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: boolean
            default: false
        - name: strategy
          in: query
          required: false
          schema:
            type: string
            enum:
              [
                SPACIOUS_LAYERED,
                BALANCED_LAYERED,
                RELAXED_SPLINES,
                VERTICAL_FLOW,
                TREE,
                RADIAL,
                FORCE,
              ]
            default: SPACIOUS_LAYERED
      responses:
        "200":
          description: Persisted view layout result
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/{level}/{id}/views/{viewId}:
    get:
      tags: [Models]
      summary: Get one stored model view
      parameters:
        - name: level
          in: path
          required: true
          schema:
            type: string
            enum: [cim, pim, psm]
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: viewId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Stored view
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/impact/{level}/{modelId}/element/{elementId}:
    get:
      tags: [Impact]
      summary: Analyze model element impact
      parameters:
        - name: level
          in: path
          required: true
          schema:
            type: string
            enum: [cim, pim, psm]
        - name: modelId
          in: path
          required: true
          schema:
            type: string
        - name: elementId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Element impact graph containing focal element, upstream chain, downstream tree, and connected peers
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/impact/artifact/{artifactId}:
    get:
      tags: [Impact]
      summary: Analyze generated artifact lineage
      parameters:
        - name: artifactId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Artifact metadata and ordered upstream model ancestors
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/chatbot/conversations:
    get:
      tags: [Assistant]
      summary: List recent assistant conversations
      responses:
        "200":
          description: Recent conversations
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/sessions:
    post:
      tags: [Assistant]
      summary: Create assistant session
      responses:
        "200":
          description: Created assistant session
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/sessions/{sessionId}:
    parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
    delete:
      tags: [Assistant]
      summary: Clear assistant session memory
      responses:
        "200":
          description: Session cleared
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/sessions/{sessionId}/messages:
    parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Assistant]
      summary: Send assistant message
      responses:
        "202":
          description: Durable assistant turn accepted
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/sessions/{sessionId}/attachments:
    parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Assistant]
      summary: Upload assistant attachment
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Uploaded attachment metadata
        "401":
          $ref: "#/components/responses/Unauthorized"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
  /api/chatbot/sessions/{sessionId}/events:
    parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Assistant]
      summary: Open assistant SSE event stream
      responses:
        "200":
          description: Server-sent event stream
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/sessions/{sessionId}/thread:
    parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Assistant]
      summary: Get durable assistant thread history
      responses:
        "200":
          description: Assistant thread snapshot
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/turns/{turnId}:
    parameters:
      - name: turnId
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Assistant]
      summary: Get durable assistant turn status
      responses:
        "200":
          description: Durable turn status
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/turns/{turnId}/cancel:
    parameters:
      - name: turnId
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Assistant]
      summary: Persist cancellation for a durable turn
      responses:
        "202":
          description: Cancellation accepted
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/turns/{turnId}/continue:
    parameters:
      - name: turnId
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Assistant]
      summary: Continue remaining work from a saved turn
      responses:
        "202":
          description: Continuation accepted
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/turns/{turnId}/undo:
    parameters:
      - name: turnId
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Assistant]
      summary: Apply a safe inverse checkpoint
      responses:
        "200":
          description: Checkpoint undone
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/turns/{turnId}/confirm:
    parameters:
      - name: turnId
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Assistant]
      summary: Confirm a pending destructive assistant batch
      responses:
        "202":
          description: Confirmed turn accepted
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/chatbot/turns/{turnId}/events:
    parameters:
      - name: turnId
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Assistant]
      summary: Replay authenticated durable assistant events
      responses:
        "200":
          description: Server-sent event stream
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/me:
    get:
      tags: [Admin]
      summary: Get current admin identity
      responses:
        "200":
          description: Current admin identity
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/bootstrap:
    post:
      tags: [Admin]
      summary: Bootstrap the first admin user
      responses:
        "200":
          description: Bootstrap result
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/overview:
    get:
      tags: [Admin]
      summary: Get admin overview
      responses:
        "200":
          description: Admin overview
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/users:
    get:
      tags: [Admin]
      summary: List users
      responses:
        "200":
          description: Users
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/users/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Admin]
      summary: Get user detail
      responses:
        "200":
          description: User detail
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      tags: [Admin]
      summary: Permanently delete a guest account and its owned projects
      responses:
        "204":
          description: Guest deleted
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/users/{id}/roles:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Admin]
      summary: Grant a user role
      responses:
        "200":
          description: Role granted
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/users/{id}/roles/revoke:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Admin]
      summary: Revoke a user role
      responses:
        "200":
          description: Role revoked
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/users/{id}/disable:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Admin]
      summary: Disable a user
      responses:
        "200":
          description: User disabled
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/users/{id}/enable:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Admin]
      summary: Enable a user
      responses:
        "200":
          description: User enabled
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/users/{id}/sessions/revoke:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Admin]
      summary: Revoke user sessions
      responses:
        "200":
          description: Sessions revoked
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/projects:
    get:
      tags: [Admin]
      summary: List projects
      responses:
        "200":
          description: Projects
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/models:
    get:
      tags: [Admin]
      summary: List models
      responses:
        "200":
          description: Models
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/jobs:
    get:
      tags: [Admin]
      summary: List jobs
      responses:
        "200":
          description: Jobs
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/jobs/{id}/cancel:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Admin]
      summary: Cancel a job
      responses:
        "202":
          description: Job cancellation accepted
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/assistant/turns:
    get:
      tags: [Admin]
      summary: List assistant turns
      responses:
        "200":
          description: Assistant turns
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/assistant/turns/{id}/cancel:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Admin]
      summary: Cancel an assistant turn
      responses:
        "202":
          description: Assistant turn cancellation accepted
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/audit-events:
    get:
      tags: [Admin]
      summary: List audit events
      responses:
        "200":
          description: Audit events
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/metrics-summary:
    get:
      tags: [Admin]
      summary: Get metrics summary
      responses:
        "200":
          description: Metrics summary
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/telemetry/frontend:
    post:
      tags: [Telemetry]
      summary: Record frontend telemetry
      security: []
      responses:
        "202":
          description: Telemetry accepted
  /api/chatbot/turns/{turnId}/rebase:
    post:
      tags: [Assistant]
      summary: Rebase a non-overlapping durable assistant turn
      responses:
        "202": { description: Rebased turn accepted }
  /api/chatbot/turns/{turnId}/checkpoints/{checkpointId}/rollback:
    post:
      tags: [Assistant]
      summary: Roll back a durable assistant checkpoint
      responses:
        "200": { description: Checkpoint rolled back }
  /api/chatbot/turns/{turnId}/feedback:
    post:
      tags: [Assistant]
      summary: Record assistant-turn feedback
      responses:
        "202": { description: Feedback accepted }
  /api/chatbot/sessions/{sessionId}/proposals/{proposalId}:
    get:
      tags: [Assistant]
      summary: Get an assistant proposal
      responses:
        "200": { description: Assistant proposal }
  /api/chatbot/sessions/{sessionId}/proposals/{proposalId}/undo:
    post:
      tags: [Assistant]
      summary: Undo an assistant proposal
      responses:
        "200": { description: Proposal undone }
  /api/admin/assistant/turns/{id}/provider-calls:
    get:
      tags: [Admin]
      summary: List provider calls for an assistant turn
      responses:
        "200": { description: Provider-call audit }
  /api/admin/landing-page-visits:
    get:
      tags: [Admin]
      summary: List landing page visits
      responses:
        "200": { description: Landing page visits }
  /api/admin/notation/{level}:
    get:
      tags: [Admin]
      summary: Get notation configuration
      responses:
        "200": { description: Notation configuration }
    post:
      tags: [Admin]
      summary: Update notation configuration
      responses:
        "200": { description: Notation configuration updated }
  /api/admin/theme-profiles:
    get:
      tags: [Admin]
      summary: List theme profiles
      responses:
        "200": { description: Theme profiles }
    post:
      tags: [Admin]
      summary: Create a theme profile
      responses:
        "200": { description: Theme profile created }
  /api/admin/theme-profiles/{id}:
    post:
      tags: [Admin]
      summary: Update a theme profile
      responses:
        "200": { description: Theme profile updated }
  /api/admin/theme-profiles/{id}/activate:
    post:
      tags: [Admin]
      summary: Activate a theme profile
      responses:
        "200": { description: Theme profile activated }
  /api/admin/theme-profiles/{id}/delete:
    post:
      tags: [Admin]
      summary: Delete a theme profile
      responses:
        "200": { description: Theme profile deleted }
  /api/admin/user-login-events:
    get:
      tags: [Admin]
      summary: List user login events
      responses:
        "200": { description: User login events }
  /api/theme:
    get:
      tags: [Admin]
      summary: Get active theme
      responses:
        "200": { description: Active theme }
components:
  securitySchemes:
    AuthToken:
      type: apiKey
      in: header
      name: X-Auth-Token
  parameters:
    ProjectId:
      name: id
      in: path
      required: true
      schema:
        type: string
    ModelId:
      name: id
      in: path
      required: true
      schema:
        type: string
    ArtifactId:
      name: id
      in: path
      required: true
      schema:
        type: string
    ModelLevelPath:
      name: level
      in: path
      required: true
      schema:
        type: string
        enum: [cim, pim, psm]
    WildcardPath:
      name: path
      in: path
      required: true
      schema:
        type: string
      description: Wildcard path handled by a planned endpoint placeholder.
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      schema:
        type: string
        maxLength: 256
      description: Replays the original async MDE job when the request fingerprint matches.
  requestBodies:
    TransformRequest:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/TransformRequest"
  responses:
    BadRequest:
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiErrorResponse"
    Unauthorized:
      description: Missing, invalid, or expired auth session
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiErrorResponse"
    Forbidden:
      description: Authenticated user does not have permission
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiErrorResponse"
    NotFound:
      description: Record or file not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiErrorResponse"
    Conflict:
      description: Revision or state conflict
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiErrorResponse"
    PayloadTooLarge:
      description: Upload or generated payload exceeds configured limits
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiErrorResponse"
    UnsupportedMediaType:
      description: Uploaded media type or file format is not supported
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiErrorResponse"
    NotImplemented:
      description: Planned feature placeholder
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiErrorResponse"
    TransformationResponse:
      description: Transformation result
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/TransformationResponse"
    MdeJobAccepted:
      description: MDE job accepted
      headers:
        Location:
          description: Polling URL for the accepted job
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/JobResponse"
  schemas:
    ApiErrorResponse:
      type: object
      required: [message, status, timestamp, issues, errorId]
      properties:
        message:
          type: string
        status:
          type: integer
        timestamp:
          type: string
          format: date-time
        issues:
          type: array
          items: {}
        errorId:
          type: string
          description: Correlation id for support and log lookup
    HealthResponse:
      type: object
      required: [status, service, timestamp]
      properties:
        status:
          type: string
          example: UP
        service:
          type: string
          example: varka-backend
        timestamp:
          type: string
          format: date-time
    RegisterRequest:
      type: object
      required: [email, password, displayName]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
        displayName:
          type: string
          maxLength: 80
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
    UpdateMeRequest:
      type: object
      required: [displayName]
      properties:
        displayName:
          type: string
          maxLength: 80
    AuthResponse:
      type: object
      required: [token, user]
      properties:
        token:
          type: string
        user:
          $ref: "#/components/schemas/UserDto"
    UserDto:
      type: object
      required: [id, displayName, guest]
      properties:
        id:
          type: string
        email:
          type: string
          format: email
          nullable: true
        displayName:
          type: string
        guest:
          type: boolean
    SaveProjectRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        description:
          type: string
        activeModelIds:
          type: object
          additionalProperties:
            type: string
          description: Map of active model level or view keys to model ids.
    InviteRequest:
      type: object
      required: [email, role]
      properties:
        email:
          type: string
          format: email
        role:
          type: string
          maxLength: 48
    UpdateMemberRoleRequest:
      type: object
      required: [role]
      properties:
        role:
          type: string
          maxLength: 48
    ProjectRecord:
      type: object
      required: [id, name, ownerUserId, activeModelIds, members, createdAt, updatedAt]
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        ownerUserId:
          type: string
        activeModelIds:
          type: object
          additionalProperties:
            type: string
        members:
          type: array
          items:
            $ref: "#/components/schemas/ProjectMember"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ProjectMember:
      type: object
      required: [userId, email, displayName, role, addedAt]
      properties:
        userId:
          type: string
        email:
          type: string
          format: email
        displayName:
          type: string
        role:
          type: string
        addedAt:
          type: string
          format: date-time
    ModelLevel:
      type: string
      enum: [CIM, PIM, PSM]
    SaveModelRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        model:
          description: CIM, PIM, or PSM model JSON.
        projectId:
          type: string
        expectedRevision:
          type: integer
          format: int64
          description: Required for PUT updates.
    PatchModelRequest:
      type: object
      required: [expectedRevision]
      properties:
        name:
          type: string
        operations:
          type: array
          items:
            $ref: "#/components/schemas/ModelPatchOperation"
        expectedRevision:
          type: integer
          format: int64
    ModelPatchOperation:
      type: object
      required: [op, path]
      properties:
        op:
          type: string
          enum: [add, replace, remove]
        path:
          type: string
          description: JSON Pointer path.
        value:
          description: JSON value for add or replace.
    ExportRequest:
      type: object
      properties:
        name:
          type: string
        model:
          description: Model JSON used by unsaved export.
        format:
          type: string
          enum: [json, xmi]
          default: json
    ModelSummary:
      type: object
      required: [id, projectId, level, name, revision, createdAt, updatedAt]
      properties:
        id:
          type: string
        projectId:
          type: string
        level:
          $ref: "#/components/schemas/ModelLevel"
        name:
          type: string
        revision:
          type: integer
          format: int64
        metamodelVersion:
          type: string
        migrationState:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ModelRecord:
      allOf:
        - $ref: "#/components/schemas/ModelSummary"
        - type: object
          properties:
            modelJson:
              description: Stored model JSON.
            metamodelHash:
              type: string
            sourceXmiHash:
              type: string
    ValidationResult:
      type: object
      required: [valid, issues]
      properties:
        valid:
          type: boolean
        issues:
          type: array
          items:
            $ref: "#/components/schemas/ValidationIssue"
    ValidationIssue:
      type: object
      properties:
        severity:
          type: string
        constraint:
          type: string
        issueClass:
          type: string
        message:
          type: string
        guidance:
          type: string
        elementId:
          type: string
        elementName:
          type: string
    ImportResult:
      type: object
      required: [name, modelJson, issues]
      properties:
        name:
          type: string
        modelJson:
          description: Imported model JSON.
        issues:
          type: array
          items:
            $ref: "#/components/schemas/ValidationIssue"
    TransformRequest:
      type: object
      required: [sourceModelId]
      properties:
        sourceModelId:
          type: string
        expectedRevision:
          type: integer
          format: int64
    TransformationResponse:
      type: object
      required: [success, status, diagnostics]
      properties:
        success:
          type: boolean
        status:
          type: string
          example: SUCCEEDED
        resultModelId:
          type: string
        resultArtifactId:
          type: string
        model:
          allOf:
            - $ref: "#/components/schemas/ModelRecord"
        artifact:
          allOf:
            - $ref: "#/components/schemas/ArtifactRecord"
        diagnostics:
          type: array
          items:
            type: string
    ArtifactRecord:
      type: object
      required: [id, projectId, name, modelJson, files, createdAt, updatedAt]
      properties:
        id:
          type: string
        projectId:
          type: string
        name:
          type: string
        modelJson:
          description: Source PSM model JSON.
        files:
          type: object
          additionalProperties:
            type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    SaveFileRequest:
      type: object
      required: [path]
      properties:
        path:
          type: string
        content:
          type: string
    JobResponse:
      type: object
      required: [id, status]
      properties:
        id:
          type: string
        status:
          type: string
          enum: [QUEUED, RUNNING, SUCCEEDED, FAILED, CANCELLED]
    MdeJobRecord:
      type: object
      required:
        [
          id,
          projectId,
          userId,
          sourceModelId,
          sourceLevel,
          sourceRevision,
          operation,
          status,
          progressPercent,
          diagnostics,
          createdAt,
        ]
      properties:
        id:
          type: string
        projectId:
          type: string
        userId:
          type: string
        sourceModelId:
          type: string
        sourceLevel:
          $ref: "#/components/schemas/ModelLevel"
        sourceRevision:
          type: integer
          format: int64
        sourceModelHash:
          type: string
        operation:
          type: string
          enum: [CIM_TO_PIM, PIM_TO_PSM, PSM_TO_ARTIFACT, VALIDATE]
        status:
          type: string
          enum: [QUEUED, RUNNING, SUCCEEDED, FAILED, CANCELLED]
        progressPercent:
          type: integer
          minimum: 0
          maximum: 100
        resultModelId:
          type: string
        resultArtifactId:
          type: string
        diagnostics:
          type: array
          items:
            type: string
        validationResult:
          $ref: "#/components/schemas/ValidationResult"
        timings:
          type: object
          additionalProperties:
            type: integer
            format: int64
        idempotencyKey:
          type: string
        fingerprint:
          type: string
        createdAt:
          type: string
          format: date-time
        startedAt:
          type: string
          format: date-time
        finishedAt:
          type: string
          format: date-time
    LayoutRequest:
      type: object
      properties:
        viewId:
          type: string
        profile:
          type: string
        preserveExistingPositions:
          type: boolean
        fixedNodeIds:
          type: array
          items:
            type: string
        options:
          type: object
          additionalProperties: true
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/LayoutNode"
        edges:
          type: array
          items:
            $ref: "#/components/schemas/LayoutEdge"
    LayoutNode:
      type: object
      required: [id, width, height]
      properties:
        id:
          type: string
        label:
          type: string
        width:
          type: number
          format: double
        height:
          type: number
          format: double
        x:
          type: number
          format: double
        y:
          type: number
          format: double
        ports:
          type: array
          items:
            $ref: "#/components/schemas/LayoutPort"
    LayoutPort:
      type: object
      required: [id, width, height]
      properties:
        id:
          type: string
        label:
          type: string
        width:
          type: number
          format: double
        height:
          type: number
          format: double
        x:
          type: number
          format: double
        y:
          type: number
          format: double
    LayoutEdge:
      type: object
      required: [id, sourceNodeId, targetNodeId]
      properties:
        id:
          type: string
        label:
          type: string
        sourceNodeId:
          type: string
        targetNodeId:
          type: string
        sourcePortId:
          type: string
        targetPortId:
          type: string
    LayoutResponse:
      type: object
      required: [nodes, edges, warnings]
      properties:
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/LaidOutNode"
        edges:
          type: array
          items:
            $ref: "#/components/schemas/RoutedEdge"
        warnings:
          type: array
          items:
            type: string
    LaidOutNode:
      type: object
      required: [id, x, y, width, height]
      properties:
        id:
          type: string
        x:
          type: number
          format: double
        y:
          type: number
          format: double
        width:
          type: number
          format: double
        height:
          type: number
          format: double
    RoutedEdge:
      type: object
      required: [id, sections, bendPoints]
      properties:
        id:
          type: string
        sections:
          type: array
          items:
            $ref: "#/components/schemas/EdgeSection"
        bendPoints:
          type: array
          items:
            $ref: "#/components/schemas/LayoutPoint"
    EdgeSection:
      type: object
      required: [startPoint, endPoint, bendPoints]
      properties:
        startPoint:
          $ref: "#/components/schemas/LayoutPoint"
        endPoint:
          $ref: "#/components/schemas/LayoutPoint"
        bendPoints:
          type: array
          items:
            $ref: "#/components/schemas/LayoutPoint"
    LayoutPoint:
      type: object
      required: [x, y]
      properties:
        x:
          type: number
          format: double
        y:
          type: number
          format: double
