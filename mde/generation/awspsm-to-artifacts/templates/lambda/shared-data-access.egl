[% /* Generates the shared data access artifact. */ %]package shared

type DataAccess struct {
	Config GeneratedConfig
	Logger Logger
}

func NewDataAccess(configObj GeneratedConfig, loggerObj Logger) DataAccess {
	return DataAccess{Config: configObj, Logger: loggerObj}
}
