# Test Fixtures

This directory is generated so generated test suites have a stable location for project-owned fixtures.

Add manually curated fixtures in protected/manual workflows only when they cannot be derived from the PSM model.
