{
  "name": "[%= emitCtx.get("projectSlug") %]",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p tsconfig.json",
    "test": "tsx --test \"tests/**/*.test.ts\"",
    "validate:contracts": "bash scripts/validate-contracts.sh",
    "sam:validate": "sam validate --lint"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
